# SDGMart — Handoff / Continuation Guide

A same-day grocery web app for Tamale, Ghana. This doc lets a new chat (or you) pick up exactly where we left off. Read this top-to-bottom once, then keep it as reference.

---

## ⭐ LATEST STATE — resume here (updated 2026-09-05)

- **(2026-09-05) v98 — the catalogue got its photos, its missing products, and an editable category list; plus the Supabase advisors triaged.** All pushed. `npm test` **80 → 111 assertions** (`tests/categories.test.js` is new). Shop is **133 products, 125 with a photo, 15 categories**.

  - **⚠️ PAYSTACK IS STILL ON TEST KEYS.** Unchanged for three sessions and still the single most urgent thing. Real customers cannot pay. Swap `PAYSTACK_PUBLIC_KEY`/`PAYSTACK_SECRET_KEY` back to the `pk_live_`/`sk_live_` pair on Render and confirm with `curl -s https://sdg-mart.com/api/paystack/config`.

  - **⭐ RECOVERING FROM A DEAD CATALOGUE RESTORED THE PRODUCTS BUT NOT THE CATEGORIES.** The owner reported every product present but the category strip and the "Shop by category" tiles gone. This was a gap in the v97 fix, not a new bug: `hooks.js` now defaults `window.CATEGORIES` to `[]` so a failed `/data/products.js` cannot crash the app, and `refreshCatalog()` then repopulated **products only** — `/api/catalog` returned `{products, showFreshness, showStock}` and nothing else. So a device whose catalogue script never loaded ended up with 132 products and zero categories: everything rendered except the way to browse it. `/api/catalog` now carries categories, neighbourhoods, essentials, top-ids and terms version, and `App.jsx` restores each one it actually receives. The lesson worth keeping: **a recovery path has to carry everything the thing it replaces carried**, or it converts a total failure into a subtle one, which is harder to spot.

  - **⭐ PRODUCT PHOTOS: 93 → 125 of 133.** From a folder of 45 images with names that only approximately matched. Fuzzy scoring got two pairs wrong — Cadbury Richoco tied with Quaker White Oats on the shared "500g" token, and the two St Louis sugar cubes nearly swapped — so the mapping was frozen as an explicit id→file table and the images opened by eye before uploading. 26 fills (products that had no photo), then 12 replacements of existing photos, each with the previous URL snapshotted first and the old storage objects left in place so a rollback restores a link that still resolves. The two malt files were decided by looking, not by filename: both are Malta Guinness (can and plastic bottle), **not** Beta Malt, which is what the scorer had guessed.

  - **⭐ SEVEN PRODUCTS THE SHOP DID NOT HAVE, AND A NEW CATEGORY FOR THREE OF THEM.** The image folder turned out to contain items with no product at all — 3 Flora paper goods, 2 Geisha soap variants, Indomie Hungry Man, and an Ideal 410g tin. Added as #183–#188, plus **`Household & Cleaning`**, which ships with its three paper products so it was never on the nav empty. The Ideal tin says "NOW 410g", i.e. Nestlé's upsized pack, so **#89 was updated from 390ml rather than a near-duplicate created** — price deliberately left at GHS 25, because repricing a live product is a commercial decision. **A correction I had to make mid-task:** I proposed creating the unpriced Flora items at `stock: 0` "so they cannot sell". That is wrong, and STEP 10 item 1291 had already proved it that morning — with `deduct_stock` OFF a `stock: 0` product still sells (201). Stock is advisory in the supplier model. They were held back until the owner priced them instead.

  - **⭐ A BAR OF SOAP WAS FILED UNDER "RICE & GRAINS".** Geisha Black Soap (#176), found while checking categories. Nobody browsing Toiletries could reach it and anyone browsing rice was shown soap. Moved. This one row is the argument for everything in the next entry: a wrong category is invisible until someone goes looking.

  - **⭐ CATEGORIES ARE NOW ADMIN-EDITABLE (Admin → Settings → Categories).** They were a hardcoded array, so every change was a code edit and a deploy. They live in `app_config` now; `DEFAULT_CATEGORIES` survives only as the fallback for a shop that has never saved a list. **The list was never the hard part.** A product stores its category as PLAIN TEXT, not a foreign key, so dropping a name orphans every product carrying it — gone from the nav and every category page, reachable only by search, exactly like the Geisha row. So the **server** enforces it: removing a category that still holds products is a 409 naming the count with nothing written; a rename is `{from, to}` and MOVES the products with it (renaming "Drinks" without that would orphan 19); a rename to a name absent from the saved list is a 400; blank, duplicate, non-string, over-length and empty lists are all refused with the previously saved list intact. `GET /api/admin/categories` also reports categories that hold products but are missing from the list, so an orphaned row surfaces by itself. Two failure modes deliberately not introduced while removing one: a junk value in `app_config` is validated, logged and fallen back from rather than served; and the degraded `/data/products.js` path reuses the last good list from memory, because a path that runs precisely when reads are failing must not need a read. 33 new assertions, and driven in a real browser — renaming "Cooking Oil" to "Oils & Fats" moved both products and left none behind.

  - **⭐ SUPABASE ADVISORS TRIAGED — most of them should be ignored, three should not.** New migration `supabase-schema-advisors.sql` (in `scripts/migrate.js` ORDER, reversible). **Applied to production 2026-09-05**, and `verify-migrations.js` was extended to cover it — 23/23. Verified after the run that nothing broke: the three tables still read with the service key, every stock function still executes, the `set_updated_at` trigger still fires, and `exec_sql` survived being pinned while it was itself executing the migration.
    - **3 CRITICAL, real:** `schema_migrations`, `stock_holds` and `order_events` have RLS **disabled**, so they are the only tables readable *and writable* with the public anon key. `supabase-rls-fix.sql` covered what existed when it was written; these three arrived in later migrations and were missed. Consequences if abused: marking a migration applied that never ran (migrate.js would skip it forever), holding every unit of every product so the shop cannot sell, and writing to the append-only audit log — an audit trail anyone can write to is not an audit trail.
    - **~12 "Function Search Path Mutable", worth fixing:** only `exec_sql` genuinely matters — it is SECURITY DEFINER and executes arbitrary SQL. The rest are SECURITY INVOKER and lower risk. Fixed with a **loop over `pg_proc`, not a hand-written list**: writing signatures by hand means getting every argument type and DEFAULT right, and a wrong signature is not an error, it is a function silently left unpinned. (I had `search_top_queries(int)`; it is `(int, int)`.)
    - **~20 "RLS Enabled No Policy" — IGNORE. These are correct.** RLS on with no policy means anon and authenticated get nothing, which is exactly right when the server holds the `service_role` key and the browser never talks to Supabase. Do not write twenty policies to silence them.
    - **~9 unindexed foreign keys — cheap, do it:** immaterial on 26 orders, but deleting a parent row scans the whole child table, so this bites later, not now. **I over-corrected here and had to walk it back in `supabase-schema-advisors-2.sql`:** Supabase flags a TABLE, not a column, so indexing every FK on a flagged table created three indexes that an existing composite already covered — one of them (`referrals_referrer_id_idx`) an exact duplicate of `referrals_referrer_idx`. Ten were genuinely missing and stay. The check that matters when doing this: the covering index must be NON-PARTIAL, because a partial index cannot stand in for a full one — which is why `addresses_user_id_idx` and `orders_paystack_ref_idx` look redundant by column list and are not.
    - **"Unused Index" — IGNORE for now.** On 133 products the planner prefers sequential scans, so an index legitimately reads as unused. Revisit under real traffic.
    - **Worth a decision:** `exec_sql` is a permanent arbitrary-SQL function sitting in production so `scripts/migrate.js` can run. It is revoked from `public`/`anon`/`authenticated`, so only the service key can call it — but it could be dropped between migrations and recreated when needed.

  - **Board unchanged at 6 items**, all still blocked on a phone (2), staging for G-06 (3), or a real mailbox (1). Nothing this session touched them.


## v97 — 2026-09-03 (superseded by v98 above; kept in full)

- **(2026-09-03) v97 — bugs the owner found by using the shop, plus one doc bug that was silently blocking a third of the test plan.** Not committed yet. `npm test` 66 → 80 assertions (`tests/inventory-edit.test.js` is new and wired into `npm test`).

  - **⚠️ PAYSTACK IS STILL ON TEST KEYS.** Unchanged from v96 and still the most urgent thing. See that entry.

  - **⭐ ADMIN INVENTORY: SAVING A PRODUCT BLANKED THE ROW — and this is B-01 for the fourth time.** `best_before` is a Postgres `date`; an `<input type="date">` that was never filled in submits `''`, and the route passed it straight through. Postgres answered `invalid input syntax for type date: ""`, the catch turned that into a **404 `{error: …}`**, and `saveEdit` — the one admin write `76b4818` did not fix — wrote that error object into the row. The product's name went blank, its price rendered **GHS NaN**, and its missing `stock` read as 0, which set off the low-stock banner. **Every product with no Best Before date was unsaveable.** Server now normalises empty → `NULL` and refuses a malformed date with a 400 that says so, instead of a database error dressed as "not found"; `saveEdit` checks `res.ok` and a returned `id`, and on any failure says so and leaves the row exactly as the shop has it. Nothing was ever corrupted in the database — the write always failed, so no repair is needed.

  - **⭐ LOW-STOCK ALERTS FIRED WITH OWN-STOCK MODE OFF.** With `deduct_stock` OFF nothing decrements these counts and the shop sells from `stock: 0` on purpose, so every product sat at or below its threshold and the banner cried wolf about all of them. Banner, the red ⚠ and "(sold out)" are now gated on `settings.deductStock`, and the table header explains why the counts are inert. **This needed a second fix to be correct at all:** `settings` was only loaded when the Settings *tab* was opened, so Inventory would have seen the `false` default forever and suppressed the alerts even for a shop that does hold stock. It now loads on mount.

  - **⭐ THE TRACK-ORDER LIST DISAGREED WITH THE TRACKING SCREEN ON BOTH TIME AND AMOUNT.** `sdgmart_guest_orders` in localStorage was written by three places that did not agree on the fields — checkout stored `total`, track-by-code and shared-link did not — and `at` was *when this device saved the entry*, not when the order was placed. So an order first seen by code or by a shared link listed as **"GHS 0.00"** stamped with today's date, while opening tracking showed the real figures from the server. Reported on orders 115 and 116. One shape now (`placedAt` + `total` + `status`, taken from the tracking response both of those paths were already fetching and throwing away), helpers live in `hooks.js`, and **`refreshGuestOrders()` re-reads every remembered order when the list opens**, so entries already saved the old way heal themselves rather than staying wrong for good.

  - **⭐ THE LIVE MAP ON THE TRACKING PAGE HAD NEVER WORKED ON A COLD LOAD.** Only `MapPicker` lazy-loads Leaflet; `OrderTrackingPage` just checked `window.L` and returned. Arriving from a push notification or a shared tracking link — the two normal ways to reach that page — left `window.L` undefined, so the map bailed on its first line and the customer got an empty bordered box. It only ever drew if they had opened the checkout map picker earlier in the same page session, and never at all on an order with no pinned location. **Removed**, and that space now shows the order itself: items with units, quantities and line totals, then subtotal / discount / loyalty / delivery / total and whether they are paying online or the rider. **Open decision:** live rider position is gone from the customer's view (the distance-based ETA in the heading still updates every 25s). Restoring it is ~10 lines of the lazy-loader borrowed from `MapPicker` — see "how the live tracker actually works" below before deciding.

  - **Four smaller fixes.** (1) The checkout **"Add a landmark OR pin your location" warning printed twice** — `CheckoutField` renders its own `error` and the address field rendered `errors.address` again underneath. (2) Choosing **"Other" for the neighbourhood and typing an area showed the raw `__other__`** on the review step and in the WhatsApp preview; `effectiveNeighborhood` already existed and was used for submission, but three display sites still read `form.neighborhood`. (3) **Inventory search** — matches name, category, unit and id; the row being edited stays visible even when it stops matching, so a half-finished edit cannot vanish. (4) The **map accuracy warning** now tells the customer to step outside as well as to drag the pin, because GPS rarely gets a good fix indoors.

  - **Feedback and issue reports arrived with no way to answer them.** The push said only what was wrong, and Admin → Issues showed name and email but not the phone — which is the number you would actually call in Tamale. Notifications now carry name · phone; `issueReports.listAll` joins `phone`; the Issues tab shows it as a tap-to-call link plus a WhatsApp link. Same change applied to order-issue reports.

  - **One crash found while testing, worth keeping fixed.** A `/api/admin/issue-reports` response that was not an array reached `issues.length` and took the **whole admin console** to the "Something hiccuped" screen, not just that tab. Real `listAll` always returns an array so it is not reachable today, but the guard is one line and a whole-console white-screen is a bad failure mode to leave armed. The same guard was added to the products loader.

  - **⭐ STAGING-SETUP.md WAS BLOCKING 13 OF THE 20 REMAINING TEST ITEMS, AND NOBODY COULD HAVE SEEN WHY.** Step 3 listed **13** migration files to paste by hand. There are **22**. The nine missing included `supabase-schema-stock-holds.sql` — so a staging database built by following that guide would not have had `stock_available`, `hold_stock`, `release_stock_hold`, `commit_stock_hold` or `restock_items`, and **every check in STEP 10 would have failed with "function does not exist" and looked like a code bug**. Also missing: `lifetime-spend` and `rider-tokens` (the v96 squad and rider-password fixes), both `constraints` files, `updated-at`, `aggregates`, `consent`, `indexes-and-email`. Step 3 now runs `node scripts/migrate.js up`, which reads the single canonical `ORDER` in the script, plus `verify-migrations.js` to prove the objects genuinely exist rather than trusting the record. This is the same class of failure as `supabase-schema-cart.sql` being ticked in prose when it had never run.

  - **How the live tracker actually works** (asked this session; recorded so it is not re-derived). It is the rider's **real GPS**, not a simulation, but it is coarser than "live" suggests: `RiderPage` runs `navigator.geolocation.watchPosition` with `enableHighAccuracy` **only while the rider has toggled themselves online**, and throttles to one POST per ~14s plus a forced ping every 15s. The server keeps **one current position** per rider (`riders.lat/lng/last_location_at`) — it is overwritten, so there is no trail or route history. `getTrackingCached` caches the tracking response 5s, and the customer's page polls every 25s. **Worst case the customer sees a position ~45 seconds old.** The important caveat: `watchPosition` is suspended when a phone backgrounds the browser or locks the screen, so the position goes stale the moment the rider pockets the phone, and nothing surfaces `last_location_at` — a stale fix looks exactly as live as a fresh one. If rider position comes back to the customer's view, show the fix age with it.

  - **⭐ STEP 10 IS DONE — the stock-reservation functions have now executed, against the live database.** The owner chose live over staging (their spare free Supabase project is in use elsewhere; a real staging project comes with the paid tier at launch). Justified by measurement rather than optimism: all 132 products had healthy stock, so turning `deduct_stock` ON made nothing unsellable. **14/14 passed.** A hold lowers `available` and never `on_shelf` (available 100 → 98, shelf untouched); releasing restores it; 999,999 units is refused with shortfalls rather than going negative; `commit_stock_hold` moves the shelf 100 → 97 and clears the hold; `restock_items` puts it back. With own-stock mode ON: two simultaneous orders for the last unit produced exactly one 201 and one 400 naming the item, and the shelf never went negative; an expired hold frees its stock; a cancelled cash order returns it (4 → 2 → 4). **And the one that matters most — with the toggle back OFF a `stock: 0` product still sells (201), which is the supplier model the shop actually runs on.** Two findings worth keeping: `expire_stock_holds()` returning 0 is **correct** — it is a garbage collector for rows over a day old, while `stock_available` already ignores anything past `expires_at`, so stock frees itself and the sweep only tidies up; and the cancel leg needs a signed-in customer because `cancelOrder` refuses guest orders outright, which is the A-02 fix. New re-runnable check: `scripts/checks/step10-stock-reservations.js <productId>`, with `--section-a-only` (stops before the toggle is touched) and `--cleanup`. **Verified independently after the run: 132 products with zero drift, orders back to 26, no leftover holds, `deduct_stock` absent again.**

  - **⭐ ONE BAD CATALOGUE RESPONSE COULD KILL THE APP ON A DEVICE, PERMANENTLY.** The owner’s laptop showed “Something hiccuped” on every sign-in while their phone was fine, and the login page itself loaded — because it is the one screen that never reads the catalogue. `error_logs` had both halves: `AdminPage` failing on `window.PRODUCTS.map`, and the customer bundle failing on `.filter` inside a `useState` initialiser. **The service worker’s network-first branch cached responses with no status check** — the general handler three lines below it checks `res.status !== 200`, that one never did — so the 500 body from `/data/products.js` (`// error loading products`, a script that parses cleanly and defines nothing) went into CacheStorage and was replayed on every load that lost the network race. It self-heals only when the connection is good enough to win, which is exactly when it is not. Fixed in four layers: the SW stores no non-200 and serves no stored error; the failing route emits the same globals with empty values plus `CATALOG_LOAD_FAILED` and `no-store`; `hooks.js` defaults every catalogue global before any component runs; and the error boundary gained **“Clear this device’s cached copy”**, because Reload and Sign out touch neither the service worker nor CacheStorage, so someone in this state had no way out without DevTools.

  - **STILL OPEN.** Unchanged from v96 except that the staging blocker is now genuinely actionable. **All 20 remaining checklist items are blocked on something physical, not on code:** a phone (3), a real mailbox (1), a Google account and a real device (2), and **staging (13 — STEP 10's 9, G-06's 3, and the optional own-stock toggle)**. Staging is the one that unblocks most, it is ~20 minutes, and its guide is now correct — but it needs a Supabase project created by hand in the dashboard, which only the owner can do.


## v96 — 2026-09-03 (superseded by v97 above; kept in full)

- **(2026-08-31 → 2026-09-03) v96 — THE TEST PLAN WAS ACTUALLY RUN. 27 commits, `main` at `76b4818`, all deployed.** v95 built the fixes and verified them against stubs; this session executed them against the live shop and **found eleven real bugs the audit had missed**. Every one is fixed, deployed and covered by a re-runnable check. `npm test` went **42 → 66 assertions**.

  - **⚠️⚠️ PAYSTACK IS STILL ON TEST KEYS (`pk_test_1df0c539…`). REAL CUSTOMERS CANNOT PAY YOU.** This is the single most urgent thing to undo. Swap `PAYSTACK_PUBLIC_KEY` and `PAYSTACK_SECRET_KEY` back to the `pk_live_`/`sk_live_` pair on Render, then confirm with `curl -s https://sdg-mart.com/api/paystack/config`. Everything that needed test keys is finished.

  - **⭐ THE BIGGEST FIND: every daily job had been dead since the v95 deploy.** `db.appConfig.claim()` threw on **every** call — `app_config.value` is `jsonb` and the filter passed a bare date, so PostgREST read `2026` as a number and choked on `-08`. `claim()` is the first thing `runDailyJobs()` does, so everything behind it stopped **silently** (the outer handler only `console.warn`s): the retention sweep (42 expired sessions, oldest 2026-06-01), the **stuck-order watchdog and its SLA alerts**, recurring orders, birthday pushes, the leaderboard award, and the hold sweeps. Fixed in `90f0add`; verified live — `daily_job_last_run` moved `2026-08-29 → 2026-08-31` and sessions went 45 → 3 on the first real run.

  - **⭐ THE ADMIN SCREENS REPORTED SUCCESS THEY NEVER CONFIRMED — this is B-01 again, where the audit did not look.** The owner deleted a product and it stayed on sale. `removeProduct` swallowed every failure and filtered the row out regardless; `fetch` does not reject on 4xx/5xx, so even a 500 sailed through and the product **vanished from the admin table while remaining in the shop**. `addProduct` never checked `res.ok`, so a rejected product was added to the table with no id — pressing Remove on it sent `DELETE /api/products/undefined`, which is what the error log showed. Its catch also fabricated a row with `id: Date.now()`, a product existing only in that browser. **Two more paths had the same shape**: `updateOrderStatus` could show an order as Delivered that the database still had as Queued, and `deleteOrder` hid orders it had not deleted. All five now check the response and leave the row alone on failure (`76b4818`).

  - **⭐ THE SQUAD REWARD PAID OUT TO PEOPLE WITH NO SQUAD, and the tier reward was a lottery.** Every signup gets its own `squad_code`, so an uninvited shopper was a squad of one and `every(member hit 500)` was just "did I hit 500?" — 5 of 6 squads were solo. Separately, `users.total_spent` was doing two incompatible jobs: lifetime spend for the GHS 50-per-1,000 tier, and progress in the squad round, which **resets**. The reset destroyed the tier: two customers spending an identical GHS 1,210 earned **GHS 100 and GHS 50** depending only on how they split their orders. Fixed with `MIN_SQUAD_MEMBERS = 5` and a new non-resetting `users.lifetime_spent` (migration `supabase-schema-lifetime-spend.sql`, backfilled from delivered-order subtotals — all 8 customers matched order history exactly). **Note the economics changed:** a member of a full active squad now earns ~10% back (5% tier + 5% squad). That is steep for groceries and is a pricing decision left open.

  - **⭐ RIDERS COULD NOT CHANGE OR RECOVER A PASSWORD AT ALL.** `/api/auth/change-password` is `customerOnly` (403 for riders) and forgot-password looked only in `users`, so a rider's reset silently matched nothing — they were stuck with whatever an admin typed at `createRider`, permanently. Built `POST /api/rider/change-password` plus rider support in forgot/reset. **The dangerous part was the shared id space (A-01):** `email_tokens` held a bare `user_id`, so a token for rider #N would have reset **customer #N's** password. Rider tokens now have their own owner column and purpose, with a CHECK enforcing exactly one owner (migration `supabase-schema-rider-tokens.sql`). Proven by building the collision deliberately — a customer and a rider both on id 56 — and confirming the rider's reset changed only the rider.

  - **Five smaller fixes, each found by a test that failed for a real reason.** (1) The search box had **no focus ring**: an inline `outline: 'none'` in `Header.jsx` beat the `:focus-visible` rule three lines below it. (2) The **last CSP violation** was Google Sign-In's stylesheet, missing from `style-src` — the policy can now move from report-only to enforced. (3) Recurring-order input mistakes answered **500 "Something went wrong on our end"** and filed a 500 in `error_logs` for a mistyped date; now 400 with the real reason. (4) **Cloudflare replaces 5xx bodies**, so a Paystack rejection reached the shopper as a bare `error code: 502` — and `CheckoutPage` then threw parsing it, telling them "Check your connection" for an email typo. Now 422, which passes through intact. (5) The **webhook had no amount-mismatch check** — the path that runs precisely when nobody is watching a screen. Now shared with `/verify` via `assertChargeMatchesOrder`.

  - **Three observability fixes, because the first two of these were found by not being able to diagnose something.** `unhandledRejection` recorded an **empty stack** (Supabase errors are plain objects, not `Error`s), so a real fault said "something broke" and nothing else — it now keeps PostgREST's code/details/hint and synthesises a stack. Crash and rejection rows carried **no status and no path**, making the most serious entries in Admin → Errors the only ones that could not be filtered for — now `path: 'process'`, `status: 500`. And the webhook now writes a **`WEBHOOK RESCUE`** line when it creates an order, because a rescued order is otherwise byte-identical to a normal one and the safety net was unobservable.

  - **What is PROVEN now, live, that was not before.** Checkout tells the truth when it fails (cart kept, no fake tracking code). Five concurrent submits produce **one** order. Loyalty cannot be spent twice. Cancelling returns exactly what it took. Rewards land on delivery, not checkout. Paystack reserves credit before the popup and releases it after 30 minutes. **The webhook safety net demonstrably fired** — order 114 was created by it, not `/verify`, and said so. A prepaid order reaches the rider as `paid: true` ("collect nothing") with `userId`/`momoNumber`/money-breakdown withheld. Revenue splits online vs rider cash correctly. Reconcile separates ABANDONED from PAID and its recovery works, refusing a second attempt with 409. Rate limits: spraying is throttled at ~50/15min per IP while a customer fumbling three passwords is not, and browsing keeps working while sign-in is blocked. An uncaught exception exits the process, non-zero, after recording the crash.

  - **⚠️ TWO MISTAKES OF MINE, recorded so they are not repeated.** (1) The step-2e admin script sent `stock: 0` in a `PUT /api/products/:id` body and restored only the price — **`PUT` is a replace, not a patch**, so it left product 37 flagged out of stock and de-listed as a bestseller, invisible to shoppers for ~2 hours. Restored from `SDGMart-catalog-import.csv`; the bestseller count going 23 → 24 confirmed the flag had been wiped. The script now snapshots the whole row and verifies the restore field by field. (2) The step-8 recovery script **auto-picked "the most recent paid order"** and, with no fresh test payment on the books, deleted a **real customer's** 13-day-old GHS 50 order. The `finally` restored it byte-for-byte, but the auto-pick is gone: an explicit id is now required and anything older than 6 hours is refused.

  - **The test board is in `TEST-CHECKLIST.md`, and its count was wrong all session until fixed.** 45 section headers still read `☐` while every child under them was ticked, so the raw count overstated the work left by nearly half. Headers now roll up from their children. **20 real items remain**, down from ~106.

  - **Re-runnable checks now live in `scripts/checks/`** — twelve scripts, each self-cleaning, most with a `--cleanup` recovery mode because a killed process skips the `finally` and leaves orders in `queued` firing SLA alerts. Start with `node scripts/checks/verify-migrations.js` (read-only, proves all 22 migrations are genuinely applied — 22/22 as of this writing by probing for the objects they create).

  - **STILL OPEN, in the order that matters.** (1) **Swap Paystack back to live keys.** (2) **Own-stock mode (STEP 10, 10 checks)** — deliberately left for last; it changes how the shop treats inventory and `deduct_stock` is currently OFF, which is the supplier model the shop actually runs on. (3) ~~**F-06 catalogue-on-resume**~~ — **closed 2026-09-03 on the installed PWA**; see the v97 entry above. (4) **G-06 migration rollback** needs staging (G-01, still open). (5) **A-11's last item** needs a real mailbox to confirm a reset email arrives. (6) **2FA on the shared admin account** — the real answer to H-04, untouched. (7) **D-11 — one Render instance only**; scale up, never out, until rate limits and caches move to Postgres.

## ▶️ HOW TO CONTINUE IN A NEW SESSION

Paste this as your first message:

> Read HANDOFF.md and TEST-CHECKLIST.md. We are at v98 and part-way through the test plan.
> Tell me the current state, then let's continue.

Everything below is what that session needs to know.

### First, five minutes of orientation — all read-only, nothing is written

```bash
cd /c/Users/Solo/Downloads/SDGMart
git log --oneline -5                              # where main is
npm test                                          # expect 111 assertions, 0 failures
node scripts/checks/verify-migrations.js          # expect 24/24 present
node scripts/checks/step2-api-checks.js           # expect 29/29, writes nothing
curl -s https://sdg-mart.com/api/paystack/config  # ⚠️ pk_live_ or pk_test_?
```

If that last one still says `pk_test_`, **stop and swap the keys back first** — real customers
cannot pay while it does.

### The checks, and what each one costs

`scripts/checks/` — every script cleans up after itself. The ones marked ⚠️ write to production.

| Script | What it proves | Cost |
|---|---|---|
| `verify-migrations.js` | All 24 migrations genuinely applied, by probing for the 50+ objects they create | read-only |
| `step2-api-checks.js` | Headers, `.git` lockdown, compression, consent, order validation, enumeration | read-only |
| `step2b-safe-checks.js` | Index plans, retention, admin flags, Accra dating, the daily-job claim marker | read-only |
| `step2d-account-checks.js` | Mixed-case sign-in, address tenancy, cancelling others' orders, recurring bounds | ⚠️ one throwaway account |
| `step2e-admin-checks.js` | Rider isolation, photo upload, price freshness, **kill switches** | ⚠️ pauses trading ~15s |
| `step3-money-paths.js` | Duplicate protection, loyalty, cancel refunds, Paystack reservation | ⚠️ **test keys only** — it refuses otherwise |
| `step3b-payment-chain.js` | Reconcile, Revenue split, the rider's view of a prepaid order | ⚠️ temp admin + rider |
| `step4-remaining.js` | Quantity caps, price integrity, one-review-per-order, the supplier model | ⚠️ throwaway account |
| `step5-regressions.js` | Sign-up/in/out, addresses, guest checkout, rider flow, recurring cron | ⚠️ throwaway accounts |
| `step6-rider-password.js` | Rider password change + recovery, and the id-collision case | ⚠️ throwaway accounts |
| `step7-rate-limits.js` | A-13/A-14/A-15 | ⚠️ **blocks this IP from signing in for 15 min** |
| `step8-paystack-recovery.js <orderId>` | PAID — NEEDS ACTION and the recovery button | ⚠️ test keys + a fresh payment |

If a run is interrupted, most take `--cleanup` to remove what they left behind. **Always run it** —
leftover orders sit in `queued` and fire SLA alerts.

### What is left, and what unblocks each

**6 items — none of them code.** Every one is blocked on something physical: a phone, a mailbox, or staging. STEP 10 (10 of the original 20) was completed against the **live** database on 2026-09-03 at the owner’s direction, and Google sign-in and push notifications were confirmed on a real account and device. **Staging now gates only G-06 rollback (3 items).** `TEST-CHECKLIST.md` is the board; its section headers roll up from their children, so
`grep -c '^- ☐'` is the honest count.

| Blocker | Items | Notes |
|---|---|---|
| ~~**Own-stock mode (STEP 10)**~~ | ~~10~~ → 0 | **DONE 2026-09-03**, against the live database. 14/14, self-cleaning, verified after the fact. See `scripts/checks/step10-stock-reservations.js`. |
| **A phone** | 2 | Outdoor GPS accuracy, plus the `accuracy`/`source` fields on the resulting order row. (F-06 catalogue-on-resume was closed on the installed PWA, 2026-09-03.) |
| **Staging** | 3 | G-06 migration rollback. Needs G-01, still open — see `STAGING-SETUP.md`, whose step 3 was **wrong until v97** (13 files listed, 22 exist) and now uses `node scripts/migrate.js up` |
| **A real mailbox** | 1 | Confirm a password-reset email actually arrives |
| **Optional / judgement** | 0 | Both settled. The squad bonus rate was decided by the owner on 2026-09-03 (tier and leaderboard prize retired). B-03’s own-stock toggle was exercised by the STEP 10 run. 2FA remains open, but it is a build, not a check — see below. |

### Three things a new session should NOT re-litigate

1. **The webhook safety net works.** Order 114 was created by it, not `/verify`, and the
   `WEBHOOK RESCUE` line in `error_logs` says so. Do not go looking for this again.
2. **Zero `PAYMENT MISMATCH` rows is meaningful**, because `npm test` section G proves the alarm
   fires when a charge and its order disagree. Silence is evidence, not an untested detector.
3. **`deduct_stock` being OFF is correct**, not a bug. The shop sells from suppliers and must keep
   selling from `stock: 0`. `npm test` order-flow section D exists to keep that true.

### Two judgement calls that WERE open — both settled 2026-09-03

Kept in full rather than deleted, because each records a decision and why it went the way it did.

- ~~**The squad bonus rate.**~~ **Decided by the owner 2026-09-03.** The GHS 50-per-1,000 tier and
  the GHS 15 leaderboard prize are retired; the squad goal, the GHS 5 referral and both delivery
  perks stay. The two kept both pay for a customer who was not there before. Credit already granted
  was deliberately NOT clawed back, and `lifetime_spent` is still maintained — it just no longer pays.
- ~~**F-06 is genuinely untested.**~~ **Closed 2026-09-03.** The owner edited a product’s name and
  price in Admin, reopened the **installed PWA** from the background, and both had already changed.
  The name is the stronger half: `refreshCatalog()` replaces `window.PRODUCTS` wholesale rather than
  patching prices, so a changed name proves the whole row propagated. First time a real catalogue
  change has been watched reaching a backgrounded PWA — the v96 attempt was invalidated because the
  “deleted product still visible” symptom was the admin write-confirmation bug, not caching.

### Habits worth keeping

- **Verify against the database after any run**, not just the script's own summary. `orders` and
  `users` counts are the fastest tell that something was left behind.
- **`PUT /api/products/:id` is a replace, not a patch.** Send the whole row back or fields get wiped.
- **Never let a script choose which production row to act on.** Pass ids explicitly.
- **A failing test is not automatically a bug.** Several this session were the test's fault — a
  basket under the GHS 50 perk threshold, `byRider` nested under `cash`, querying `user_id` for a
  token that lives in `rider_id`. Check the expectation before changing code.

---

## Earlier states (v95 and before)

## ⭐ LATEST STATE — resume here (updated 2026-08-29)
- **(2026-08-29, end of session) v95 — ALL OF THE ABOVE IS MERGED, PUSHED AND LIVE on `sdg-mart.com`.** `main` at `faa98d4`. The v93/v94 entries below describe what was built; this entry is the current truth about its state.
  - **Verified live after deploy:** service-worker stamp went `sdgmart-v92-remaining-highs` → `sdgmart-faa98d4deba3` (the G-05 automation working in production); app renders with 132 products and **zero console errors, including zero CSP report-only violations**; all six security headers present; `/.git/config` and `/.env` 404; `/healthz` and `/readyz` 200; catalogue payload **5,741 → 4,774 bytes gzipped**; signup consent checkbox live with working `/privacy` and `/terms` links.
  - **All migrations are run** — the five new ones plus the runner itself. `schema_migrations` and `exec_sql` now exist, so **`node scripts/migrate.js status` works from here on**; no more probe scripts. ⚠️ The record is empty for everything applied by hand — run `node scripts/migrate.js mark <file>` for each already-applied file so `status` stops reporting them pending.
  - **The stock SQL is no longer untested. 12/12 against the real database:** availability starts equal to the shelf, a hold drops availability while **leaving `products.stock` untouched**, over-holding is refused with a shortfall naming the product, release restores it exactly, zero rows left behind. `hold_stock`/`release_stock_hold`/`stock_available` are proven. Still unproven: `commit_stock_hold`, `consume_stock`, `restock_items` (they change `products.stock`, so they were not exercised against live data).
  - **⚠️⚠️ PAYSTACK IS ON LIVE KEYS** (`pk_live_66e3762476…`). Every payment test moves **real money**. Swap to the `pk_test_`/`sk_test_` pair on Render before testing anything on the money path, and swap back before launch. This gates most of the remaining test work, because payment touches the most fixes.
  - **⚠️ MIGRATION FILES: never paste a rollback section.** Five migrations were run by hand, reported success, and left nothing behind — because each file ended with an executable `-- ══ DOWN ══` section that `migrate.js` skips but **the Supabase SQL editor happily runs**, creating everything and then dropping it. Fixed in `faa98d4`: every DOWN section is now wrapped in `/* */` so a whole file is safe to paste, and `splitMigration()` unwraps it for `migrate.js down`. **Any future migration must follow that pattern.**
  - **⭐ CORRECTED POSITION ON G-01 — read this before repeating "blocked on staging".** The register treats "no staging database" as blocking all testing. **That is wrong for right now.** The live database holds **9 users, 23 orders, 1 rider, nothing since 2026-08-20** — there is no real customer data to protect, so it *is* effectively a staging database. G-01 genuinely blocks **CI** (an automated suite running forever needs its own database) and matters **after launch**, when a bad test would corrupt real orders and balances. It does not block sitting down and testing these fixes once, this week. The staging database arrives with the Supabase upgrade after launch; set it up properly then.
  - **⭐ NEXT SESSION STARTS HERE — the agreed test plan, in order.** (1) **Swap Paystack to test keys on Render** — everything else waits on this. (2) **Automated API-level checks** (can be scripted against the live API): rate limits and 429s, order-input validation, empty/oversized baskets, consent enforcement, `.git` lockdown, headers, email-enumeration identical-response checks. (3) **Hands-on**: offline checkout keeping the cart, double-tap on Slow 3G, admin tables on a real phone, keyboard focus, PWA resume refresh, **and confirming the service worker updates on a real phone** (never verified — the browser pane sandboxes SW registration). (4) **Money paths with test keys**: loyalty reserve/release, cancel restoring credit, delivery earning rewards, webhook retry, Reconcile, Revenue split. (5) **Own-stock mode**: turn it ON, exercise hold/refuse/restock, turn it OFF — safe now, genuinely risky once there are customers. (6) **Clean up** with `cleanup-test-data.sql`, swap Paystack back to live. Two tests briefly break the site (forcing a crash for E-06, toggling the kill switches) — fine today, unacceptable after launch, so do them now.
  - **`npm test` exists** — `tests/stock.test.js` + `tests/order-flow.test.js`, 42 assertions, no database, no framework. Run it before any push. Order-flow **section D is the one to keep green**: with own-stock mode OFF the shop must still sell from `stock: 0`, which is the supplier model the live shop runs on.
  - **STILL OPEN:** **tap "Enable admin alerts" once on the phone you carry** (Admin → Dashboard) — the new webhook/payment/stuck-order alerts reach nobody until that device is subscribed. **2FA on the shared admin account** (the real answer to H-04). **D-11** — Render paid tier is a *bigger* machine and is safe; **never add a second instance** until rate limits and caches move to Postgres. If the site is slow, scale up, not out. **G-05's CI half.** **Own-stock mode stays OFF** until step 5 above is done.
- **(2026-08-29, later still) v94 — C-10 stock reservations + G-05 half. `npm test` now exists.** Commits `df62472`, `e9043d4`.
  - **C-10 — stock is now reservable, and can go back up.** `products.stock` = what is physically on the shelf; availability = that minus unexpired holds. **Holds are taken at CHECKOUT, not add-to-cart** (a deliberate decision: carts here persist across sessions and devices, so a cart-level hold would lock the last of an item indefinitely for someone who wandered off). Paystack holds at `/api/paystack/init` keyed on the payment reference; cash does an atomic check-and-decrement *before* the commit; cancel/payment-failure/deletion **restock**, a path that previously did not exist anywhere. Pricing quotes against available stock so the last bag is never offered to two customers. Hold TTL `STOCK_HOLD_TTL_MIN` (default 15). **`supabase-schema-stock-holds.sql` must be run before own-stock mode can be turned on — the toggle now refuses without it**, and enabling it is recorded in the error log.
  - ~~⚠️ THE SQL HAS NEVER RUN.~~ **Superseded — 12/12 verified against the live database; see v95.** No Postgres or Docker on the dev machine. The application logic is covered by 42 passing assertions, but the functions themselves are unexecuted. **Run migration section 9's checks by hand on staging before ever turning the toggle on.**
  - **`npm test` — 2 files, no database, no framework.** `tests/stock.test.js` drives the real `db.stock` against a JS simulation of the SQL semantics; `tests/order-flow.test.js` boots the real `server.js` with a stubbed database. **Order-flow section D is the one to keep green: with own-stock mode OFF the shop must still sell from zero stock** — that is the supplier model the live shop runs on today. This is a partial answer to G-05: real tests exist and need no staging, though CI still does.
  - **G-05 half done — `sw.js` CACHE_NAME is no longer bumped by hand.** The server stamps the deployed build id as it serves the file (`RENDER_GIT_COMMIT` → `.git/HEAD` → newest mtime). **HANDOFF §5 step 4 is deleted, not merely discouraged.** Verified: stamps `sdgmart-<sha>` matching git HEAD.
  - **Still open from these three:** **D-11** — one Render instance only. Paid tier is a *bigger* machine and is safe; **adding a second instance is a separate setting and must not be used** until rate limits and caches move to Postgres. If the site is slow, scale up, never out. **G-05** — CI still blocked on G-01 (no staging database to run against).
- **(2026-08-29, later) v93 — THE 38 AUDIT MEDIUMS: 34 addressed in 7 batches, on `fix/audit-mediums-batch-1`.** Commits `1536dbd` → `c54ef0d`. Batches: (1) unauthenticated attack surface, (2) concurrency + dates + write validation, (3) schema hygiene + retention + rollback, (4) query performance, (5) client correctness, (6) ops + observability, (7) privacy + data quality. ~~Not merged to `main` yet.~~ **Superseded — merged, pushed and live; see the v95 entry above.**
  - **⚠️ SIX MIGRATIONS TO RUN, staging first.** `node scripts/migrate.js status`. New: `supabase-schema-constraints-2.sql` (**can fail by design** on existing duplicate order reviews — INSPECT query is in the file), `supabase-schema-updated-at.sql`, `supabase-schema-aggregates.sql`, `supabase-schema-consent.sql`. The code degrades gracefully without all of them: the aggregate functions fall back to counting in Node, and consent columns are optional in the drift check. **`supabase-schema-constraints.sql` (from v92) must be run too — it already carried the product price/stock CHECKs and the one-default-address index that B-13/B-14 depend on.**
  - **⚠️ CHECK BEFORE DEPLOYING: `select email, must_change_password from users where role='admin'`.** A-16 makes that flag *actually enforced* on admin routes (it was only ever a UI hint). If it is `true` on your account you get a 403 on admin until you change your password — sign-in and `/api/auth/change-password` stay open, so it is recoverable, not a lockout.
  - **⚠️ STILL YOURS TO DO: tap "Enable admin alerts" once, on the phone you carry** (Admin → Dashboard). Batch 6 added alerts for webhook failures, payment mismatches, mail outages and orders past SLA — **none of them reach you until that device is subscribed.**
  - **New env vars, all optional:** `ADMIN_BOOTSTRAP_PASSWORD` (else a random one is printed once at creation), `BUSINESS_TZ` (default `Africa/Accra`), `ORDER_SLA_HOURS` (default 4).
  - **Behaviour changes worth knowing.** Password hashing is now async — logins and signups no longer block the event loop. Delivery dating derives from an explicit `Africa/Accra`, not the server's clock. Riders receive a reduced order payload. `/api/me/orders` is capped at 100 (was unbounded); `?limit` overrides. New `GET /api/catalog` (JSON catalogue, for the PWA resume refresh). Signup now **requires** accepting the privacy notice — a direct POST without `acceptedTerms` is rejected. `migrate.js` gained `down <file>`.
  - **⚠️ FIVE REGISTER FINDINGS WERE WRONG OR STALE — on top of the three already corrected in v92.** (a) **A-18 was already fixed** in v92; both halves use `casUser`. What was actually missing: the drift check did not verify `users.referral_credited`, which that fix depends on — if absent, the CAS throws into a catch and referral credit silently never pays. Now checked. (b) **D-10's examples were wrong.** The catalogue *does* need `best_before` (Home, Category and Product pages all render it), and `/api/me/orders` is not "a code, a status and a total" — MyOrdersPage reads 25 fields including the full `items` array. What *was* droppable: `description`, `low_stock_threshold`, `created_at` — measured at 967 gzipped bytes off every page view. (c) **F-07 overstated the labelling gap**; Header, CartDrawer and ProductPage icon buttons already had `aria-label`. The missing focus ring was the whole story. (d) **F-08 was listed as unconfirmed suspicion — it is real.** Three of four admin tables were unreachable on a phone. (e) **H-05 was partly out of date**; Paystack and Resend were already disclosed.
  - **NOT DONE — three deliberately held back as projects, not fixes.** **C-10** (no stock reservation model — a redesign), **D-11** (per-process state blocking horizontal scaling — architectural), **G-05** (no tests or CI — needs a framework decision). Also skipped: deferring Google Analytics behind a consent notice (a product decision about first paint), and 2FA on the admin account, which is the real answer to H-04 and is still open.
  - **HOW THIS WAS VERIFIED — and its limit.** Everything was exercised against a **stubbed database** (`scratchpad/harness.js` swaps `./database` via `require.cache`) or a recording fake, plus a real browser for the client work: rate limits producing 429s, 50 logins across 50 different emails, scrypt timing at 46.4ms vs 45.8ms, cart re-pricing GHS 40 → 65, catalogue refetch 0 inside the 60s window and 1 past it, focus ring `rgb(201,89,31)`, forgot-password returning byte-identical bodies during a simulated mail outage, three admin tables given scroll. **No real user, no real database, no real payment has touched any of it.** `TEST-CHECKLIST.md` has the new section.
- **(2026-08-29) v92 — PRE-LAUNCH SECURITY & RELIABILITY AUDIT: 49 of 88 findings fixed, all deployed.** A full adversarial audit across 9 categories (security, data integrity, business logic, performance, reliability, front-end, operations, data protection, Ghana/mobile-money specifics) produced **88 findings**. Fixed and merged to `main` (commit `0937c39`, 14 commits): **all 9 criticals and 39 of the 40 highs**. Full register with every finding, file:line citation and resolution note: **https://claude.ai/code/artifact/995a1349-190d-43c9-9b0e-a27f08a2c8b2**
  - **The five that mattered most.** (1) **Checkout confirmed orders that were never saved** — `placeOrder` swallowed every failure and still showed "Order received.", cleared the cart and issued a client-generated `SDG-XXXXXX` code; `fetch` does not reject on 4xx/5xx, so a 500 slipped past the catch untouched. (2) **Loyalty could be minted for free** — rewards were granted at order creation and cancellation reversed nothing, so place-collect-cancel-repeat paid out indefinitely on Cash on Delivery, which needs no payment. (3) **A rider changing their password overwrote a customer's** — riders and customers share an id space and the route wrote to the `users` table. (4) **`.git` was served** — `send`'s legacy dotfile rule only inspects the last path segment, so `/.git/config` resolved to `allow`. (5) **The Paystack webhook returned 200 on failure**, so Paystack never retried — money taken, no order, no trace. Verified shut in production: `/.git/config` → 404.
  - **Behaviour changes you need to know.** **Rewards now accrue on DELIVERY, not checkout** (this is the fix for the free-credit loop — value the customer *spends* is still taken at checkout and is now *returned* on cancel). The success screen says "GHS X in credit lands in your account once this order is delivered" when non-zero. **Two new admin tabs:** 💰 **Revenue** (a day's takings split online vs cash, cash broken down per rider into collected/still-out) and 💳 **Reconcile** (payments with no order against them, each checked against Paystack, one-click recovery). **Settings → ⚠️ Emergency switches** can stop ordering, online payment or loyalty redemption with no deploy. **Two bundles now:** `/app.bundle.js` for shoppers (52.2 KB gzip, was 79.6) and `/app.staff.js` fetched only when an admin or rider signs in. New endpoints: `/readyz` (readiness, actually queries the DB — point a monitor at it alongside `/healthz`), `GET /api/me/export`, `POST /api/me/delete-account`.
  - **Schema.** All migrations run and verified — startup now prints `✓ Database schema matches what the code expects`. **New files:** `supabase-schema-order-idempotency.sql`, `supabase-schema-indexes-and-email.sql`, `supabase-schema-constraints.sql`, plus `scripts/migrate.js` (a real runner with a `schema_migrations` table; see its header for the one-time `exec_sql` helper). ⚠️ **The startup schema check found real drift on its first boot: the `carts` table did not exist**, despite §4 recording `supabase-schema-cart.sql` as run — so the persistent cross-device cart had never worked, hidden by a `.catch(() => {})` on the client. Now fixed. **Use `node scripts/migrate.js status` from here on, and run migrations on staging first.**
  - **⚠️ THREE AUDIT FINDINGS WERE WRONG and are corrected in the register — do not act on the original text.** (a) **F-02** claimed no screen had an error state; MyOrders, Account and OrderTracking all do, and CategoryPage has no fetch that can fail. Only SquadPage was genuinely weak (fixed). (b) **G-07** called `tweaks-panel.jsx` dead code; `App.jsx` calls `useTweaks()` for the active theme and renders `<TweaksPanel>` — removing it from the bundle crashed the app. Only its unvalidated `postMessage` listener is a real issue, still open. (c) **D-05**'s "five unused font families" was wrong — all twelve are referenced and `<body style="font-family: Roboto">`. Font trimming still to do, needs a visual pass.
  - **NOT YET TESTED END TO END.** Everything was verified by syntax check, bundle compile, logic simulation, or a live probe against a running server — **no real user has exercised these flows**. `TEST-CHECKLIST.md` lists everything outstanding, ordered by value, with the known limits recorded so they are not mistaken for failures. Highest value four: offline checkout shows the retry panel with a full cart (not a success screen); double-tap on Slow 3G produces exactly one order; cancelling an order that used loyalty returns the credit; marking an order delivered is when loyalty is earned.
  - **STILL OPEN: 1 high, 38 mediums.** The high is **G-01 — there is no staging database**, so local development writes to production; `STAGING-SETUP.md` is a step-by-step for the ~20-minute fix and is the single highest-leverage thing left. ⚠️ Also outstanding: **set `TRACK_TOKEN_SECRET` on Render** (guest tracking tokens still derive from `SUPABASE_SERVICE_KEY`, so rotating that key would break every saved tracking link — the server warns at startup until set), and **run `cleanup-test-data.sql`** to remove the `sdgtest-*` accounts and orders ~20–23 that still skew every dashboard figure (deliberately not run: it deletes real rows, has an INSPECT section first, and some ids in that range may be genuine).
- **(2026-08-28) v81 — search bar no longer dismisses the mobile keyboard after every letter.** Root cause was a **React remount**, not a keyboard setting: `SearchField` was declared *inside* `Header`, so every keystroke (`setSearchQuery` → Header re-renders) created a fresh `SearchField` function; React saw a new component *type* and **destroyed + rebuilt the `<input>`**, dropping focus → the phone keyboard closed after each character. Fix in `Header.jsx`: **hoisted `SearchField` to module scope** (stable identity → the input is reused, never remounted → focus + keyboard persist while typing), passing `searchQuery/onSearch/setSearchQuery/setPage` from BOTH call sites (desktop header row + mobile search gutter). Also added the intended *"only dismiss on Search/Enter"* behavior: wrapped in `<form role="search">` with **`enterKeyHint="search"`** (the phone keyboard's action key now reads **Search** per-OS on iOS/Android; desktop uses Enter) + `inputMode="search"`; on submit/Enter it navigates to results and **`blur()`s** the input so the keyboard closes only on that explicit action. Both the form `onSubmit` (mobile Search key) and an explicit `Enter` `onKeyDown` are wired; the × clear button is now `type="button"` (won't submit the form) and refocuses. No visual change (the `<form>` keeps the old `<div>`'s flex styling). **Verified in the live bundle:** real typed keystrokes ('tomato'/'rice'/'milk') kept the *exact same* input DOM node focused through every letter (old code remounted on letter 1); invoking the wired `onKeyDown('Enter')` and a genuine `form.requestSubmit()` both moved focus to BODY (keyboard dismisses); `enterKeyHint`/`inputMode`/`form role="search"` confirmed on the live node; no server or app-code errors. **NB — the JS-bundle gotcha:** the PWA service worker (`sw.js`) caches `/app.bundle.js`; during testing the page kept running the *previous* cached bundle until the SW cache was cleared — exactly why **`sw.js` CACHE_NAME must be bumped every release**: changing that string changes the SW file, so the browser adopts a new SW whose `activate` deletes all old caches, guaranteeing the fresh bundle is served. `sw.js` → **v81** (`sdgmart-v81-search-keyboard-fix`).
- **(2026-08-21) v80 — cart free-delivery message + Family-Mode keeps the seamless checkout.** Two UX fixes. **(1) Cart now reflects the first-order-free perk** (was: cart always showed a GHS 10 delivery fee until GHS 150, so the "first order over GHS 50 is free" perk only appeared at checkout — confusing). `CartDrawer.jsx` now takes `currentUser` (passed from BOTH render sites in `App.jsx` — the `page==='cart'` full view AND the `cartOpen` drawer overlay) and mirrors `CheckoutPage`'s delivery logic: free ≥ GHS 150 for everyone; signed-in members with `firstOrderDone===false` also get FREE the moment subtotal ≥ GHS 50. Delivery row shows **FREE** (green) with a left-accent note *"First delivery's on us."* once ≥50, or a nudge *"Add GHS X more and your first delivery is free."* while under 50; guests / returning users / logged-out see the normal GHS 10 (perk correctly hidden). Uses `subtotal` (pre-loyalty) for the preview threshold — checkout stays authoritative. **Verified via isolated `window.CartDrawer` renders across 7 states** (guest 30/160, first-order 30/60/160, returning 60, logged-out 60) — all branches correct. **(2) Family Mode no longer wipes the pre-filled details.** `CheckoutPage.jsx` `compactAddress` dropped its `!familyMode` guard (now just `appliedAddress && !editingAddress`), so flipping "ordering for someone else?" keeps the user's compact saved-address card + pre-filled name/phone exactly as-is and **only ADDS the Recipient Details section** below — the full neighborhood/address/map form no longer reappears. Delivery still defaults to the saved address; **"Change / add details"** reveals the full form when the gift goes to a different drop-off (admin/rider read the delivery loc from `location.address || address || recipientAddress`, so this is safe). **Verified via isolated `window.CheckoutPage` render** (network stubbed to a canned saved address): before toggle → compact card only; after toggle → compact card STAYS + Recipient Name/Gift Message appear + full neighborhood select stays hidden. Bundle compiles clean, zero console errors. `sw.js` → **v80**. ⚠️ Not yet eyeballed as a screenshot (Browser pane wasn't displayed for compositing) — logic fully verified in the live bundle; do a quick visual pass + real-account check post-deploy.
- **(2026-08-19) v79 — WhatsApp number change + essentials-rail fix + all 14 category tiles now have photos.** (1) **New business WhatsApp number `+233599189773`** (SIM swap from `+233504082555`) — replaced in all 8 files (14 hits): wa.me links (Header float, HomePage footer, ProductPage out-of-stock, FeedbackBox, MyOrders guest, AdminPage comms + fallback), the success-screen placeholder example, and the PDF receipt footer (`receipt.js` → "+233 59 918 9773"). The old number is fully gone (verified). (2) **Essentials rail tiles were uneven** — short-name cards were shorter with the grey rail bg showing beneath them; fixed by `.rd-ess-item{display:flex;width:236px}` + `.rd-ess-item>.rd-cell{width:100%}` in `SDGMart.html` so every card stretches to equal height (verified all 9 = 392px). (3) **Sourced 7 category tile photos** (the categories that were rendering as dark stripes): spices/baking/coffee/staples/meat/toiletries from Unsplash (cropped 600×640 q78 via sharp) + fruits-veg from the staged `hero-produce.jpg`; saved to `icons/categories/` and wired into `HomePage.jsx` `CAT_IMAGES` — **all 14 categories now have images** (verified each tile → correct src). `sw.js` → v79. (Earlier v78 fixes below: 2-step tracking strip, category-pill black-on-black, odd-cell white.)
- **(2026-08-19) DESIGN-LANGUAGE REFRESH — SLICE 1 of the rollout SHIPPED (foundation + chrome + Home).** Following the handoff bundle `Downloads/SDGMart catalogue redesign directions/design_handoff_sdgmart_refresh/` (README + `*.dc.html` prototypes + `screenshots/`). New language: flat, typographic, black + burnt-orange (`--accent #c9591f`), hairline grids, no shadows/radius, **no emoji**. **User decisions (via Q):** (1) FULLY RESPONSIVE incl. phones; (2) build the additions AND their backend so they actually work — **category Sort control** + **4-step live-tracking status strip** (Essentials "Add all" already existed); (3) roll out as **vertical slices**, deploy each; (4) **also restyle Login & Signup**. Emoji-removal treated as core (not optional). **DONE this slice:** `SDGMart.html` — added Google Fonts (Gabarito/Space Grotesk/Bricolage/Instrument Serif/IBM Plex Sans+Mono/Archivo Narrow) + a full **design-token block** (`--ink,--accent,--accent-light,--accent-amber,--rule,--rule-2,--surface-warm,--dark-rule,--f-display/-hero/-mark/-serif/-ui/-mono/-label`, etc.) + responsive utility classes (`.rd-gutter,.rd-grid(-4/-5),.rd-cats,.rd-rail,.rd-tile`). NEW tokens are separate names so un-migrated screens keep the old `--sage/--cream/--font-*` look during transition. `components/Header.jsx` — full rewrite to the new chrome (announcement bar + header + category pill rail), responsive (desktop cluster vs mobile hamburger + stacked search), drops the `theme` prop/`isV2` branch; App.jsx now passes `selectedCategory` so the rail shows the active pill. `components/HomePage.jsx` — rewritten: added **`RPrice`** (GHS/cedis/pesewas lockup), **`RCard`** (flat product cell), `RSectionHead`/`RSeeAll` (all exported to window for reuse); sections = hero (uses `/icons/hero-market.jpg` under a heavy dark overlay; 4 other staged `hero-*.jpg` are swap candidates), trust strip, Household Essentials rail + working **Add all N — GHS x**, Bestsellers 5-up, Today's Deals/Suggested/Clearance (dynamic, restyled), Shop by Category 7-up, Request-an-item, footer. **Kept the LEGACY `ProductCard` intact** (CategoryPage + ProductPage still use it until migrated). `FeedbackBox.jsx` — added a `dark` variant for the footer (Account keeps the light one). Verified in preview desktop+mobile: fonts load, Add-all → cart badge, hairline grids 5→3→2 / 7→4→2, **zero horizontal overflow**, hamburger menu. `sw.js` → **v71-design-refresh**. **ROLLOUT PROGRESS:** ✅ Slice 1 = foundation + chrome + Home (v71). ✅ tweaks: grocery-bags hero + removed Shop-essentials link (v72). ✅ Slice 2 = **browse-and-buy core (v73): ProductPage, CategoryPage (+ working Sort dropdown — Popular/Price/Name/[Expiry], all client-side over `window.PRODUCTS`, no backend needed), CartDrawer** — all rewritten to RCard/RPrice/tokens, verified desktop+mobile (sidebar 15 rows, 4-up grid, sort re-orders, cart total = subtotal+delivery, no emoji, no overflow). ✅ Slice 3 = **Login/Signup (v74)** — `LoginPage.jsx` rewritten (grocery-bags photo bg, flat white card, Space Grotesk wordmark + Instrument-Serif "delivered." accent, ink tab switcher + actions, square inputs; all auth logic kept: email/pw, Google GIS button set to `shape:'rectangular'`, forgot/reset, guest, ?ref= prefill). Also cleaned shell emoji: PWA install banner 🏪 → "SDG" mark (`SDGMart.html`), error-boundary 😞 removed (`App.jsx`). ✅ Slice 4 = **Squad + Account (v75)** — `SquadPage.jsx` (ink header panel, giant Gabarito spend totals, ink/accent progress bars, 34px initial squares, mono `01/02/03` leaderboard ranks) + `AccountPage.jsx` (flat bordered sections, square inputs, Archivo labels, mono LOCKED birthday tag, ink DEFAULT address tag, label pills) + `NotifyOptIn` restyled + **FeedbackBox emoji (💬/🙏) stripped from the light variant too**. All logic preserved (profile save, address CRUD, MapPicker, birthday lock, referral copy/share, leaderboard). Verified via mocked session + stubbed squad fetch (no real customer creds needed). ✅ Slice 5 = **Checkout + Order Received (v76)** — `CheckoutPage.jsx` restyled **presentation-only** (ALL Paystack init/verify, pricing, family-mode, scheduling, loyalty, first-order-free, validation, snapshot/receipt logic byte-for-byte preserved — only JSX + the CheckoutField/inputStyle helpers changed): hairline 3-cell stepper (active ink + amber "Step N of 3"), 380px flat Order Summary with `RPrice` Total + warm left-accent "add more" note, delivery/review/pay steps, and the success screen (2px rule + accent eyebrow + Gabarito "Order received." + Instrument-Serif line + warm tracking-code panel + 2×2 hairline action grid; WhatsApp cell green). No emoji. Verified all 3 steps render + navigate (validate1 works) + pricing correct (Total 135.00 = 125 + 10). ⚠️ Did NOT place a live order in preview (avoids production order row + admin push) — success screen is presentation-only, tag-balanced, compiles. ✅ Slice 6 = **My Orders + Order Tracking (v77)** — `OrderTrackingPage.jsx` (mono order id, Gabarito status heading, **4-step status strip** Placed/Queued-for-dispatch/Rider-on-the-way/Delivered that reads the live `order.status` — top-rule ink=done/accent=current/rule pending, mono times: real for placed=createdAt + delivered=deliveredAt, middle steps show Done/Now; 2.1-aspect map area with a dark-dot rider marker; 3-cell Address/Delivery/Order-total footer) + `MyOrdersPage.jsx` (guest track-code entry with flush mono input + ink Track, dashed empty state; signed-in order cards with typographic status badges [Queued=warm/amber, Delivered=ink], uppercase text actions, recurring block, restyled issue modal). No emoji (incl. `notify()` OS titles). Verified via seeded guest order + stubbed in_transit tracking fetch (strip → "Rider on the way = Now", footer + total correct). **🎉 DESIGN-REFRESH ROLLOUT COMPLETE — the home page + all 12 documented screens are on the new language and LIVE.** Total: v71→v77. **(v78 post-launch fixes, LIVE):** (1) tracking strip **trimmed from 4 steps to the 2 DB-backed milestones** — Order placed (`createdAt`) + Delivered (`deliveredAt`) — at the user's request (removed the two timeless middle steps; live progress still shown by the status heading + map). This makes the `status_history` migration below moot unless a 4-step strip is ever revived. (2) Fixed the category-rail **active pill rendering black-on-black** (the `.rdh-pill:hover{color:var(--ink)!important}` rule was hiding the white label on the selected black pill, esp. after a touch-tap sticks `:hover`) — active pills now omit the `rdh-pill` class. (3) `.rd-grid` reworked from `gap:1px`+grey-bg to **per-cell borders over a white (`--panel`) ground** in `SDGMart.html`, so a half-filled last row (odd item count, common on 2-col mobile) shows a plain-white empty cell instead of the grey gutter. **Optional future polish:** the tracking strip's two MIDDLE steps show Done/Now without an exact time; giving them real timestamps would need a small `orders.status_history` (JSONB) migration + `db.orders.setStatus` appending `{status, at}` — not built (kept the strip migration-free; it's fully functional off `order.status`). ~~[original NEXT plan follows, now all shipped]~~ track-order entry (screen 08, in MyOrders' `GuestOrdersView`), **live tracking (screen 09 — build the 4-step status strip Placed/Queued/On-the-way/Delivered + BACKEND status timestamps: orders only has `delivered_at`; add a status-history or per-status timestamp so the strip shows real times)** in `OrderTrackingPage.jsx`, and **my orders (`MyOrdersPage.jsx`)** (best grouped with the tracking screens — guest tracking-code entry lives in MyOrders' `GuestOrdersView`). Each: reuse `RCard`/`RPrice`/tokens, switch product grids to `.rd-grid`, drop emoji, bump `sw.js`, redeploy. Per-screen specs in README "Screens" + exact markup in `SDGmart Flow 2b.dc.html` (screens 04–12 at lines ~198–740) + `screenshots/NN-*.png`.
- **(2026-08-19) BLACK-BACKGROUND CLEANUP — 22 photos fixed to white, LIVE.** User flagged that some catalogue photos sat on black backgrounds (Konzoom's packshots come that way). Detected via border-ring luminance scan (`detect-black-bg.js` → `black-bg.json`): **22 of 103**. Fixed with an **edge flood-fill** (background-connected black → white; interior black — labels/text/dark packaging — preserved) + 2-pass halo softening (`process-black-bg.js` → `processed/<id>.jpg`), self-verified visually incl. the hard cases (Colgate **Charcoal** black-on-black, Storm dark can — both came out clean), then uploaded via `scripts/upload-photos.js --overwrite`. Before/after sheet: https://claude.ai/code/artifact/f2837b84-c786-443a-8c4c-3caa4582d946 . **The flood-fill is now baked into `scripts/upload-photos.js` as an opt-in `--white-bg` flag** for any future black-bg sources. Ids fixed: 38,47,56,57,59,62,65,68,78,87,94,104,113,117,119,120,123,127,130,158,162 (**21 live**). Originals remain in the bucket (revertible via `black-bg.json` `origUrl`). Photo count unchanged (103/132) — same photos, white bg. **168 Colgate Charcoal was REVERTED to its original black bg at user request** (its box's right edge had eroded in the black-on-black flood-fill) — `products.img` pointed back to the original storage file.
- **(2026-08-19) PHOTO REVIEW ROUND 1 DONE → 8 uploaded, now 103/132; 29 still blank.** User reviewed the page and picked 8 correct matches — **41** Fortune rice, **81** Ideal evap milk, **83** One Yogo, **90** Dano UHT, **108** Don Simon, **151** Cucumber, **152** Lemon, **154** Okra — all compressed (sharp 900px JPEG, transparent PNG flattened on white), uploaded to `product-photos` bucket, `products.img` set + verified public 200. Result map: scratchpad `upload-result.json`; uploader `upload-picks.js`. **29 STILL MISSING** (user marked "None fit" — source feeds lack the brand or had wrong variant/product): 42 Abena rice, 43 Cindy rice, 60 Aunty Lulu shito, 64 Aunty Lulu green chili, 67 Heinz ketchup, 70 Amoy soy, 71 Diamir mayo, 75 Remie seasoning, 77 Yanni stew, 95 Kalyppo, 101 Kalyppo multifruit, 102 Awake water, 103 Vita Milk, 105 Ceres red grape, 109 Ocean Spray cranberry, 111 Soda crackers, 112 Welch's fruit snacks, 114 Munchee potato cracker, 121 HoH biscuit, 124 Quaker oats, 129 Bruggen corn flakes, 131 St Louis cube sugar, 135 Milo refill, 136 Cadbury Richoco, 137 Golden Tree cocoa powder, 138 Tetley decaf, 155 Orange, 156 Yam. **DECISION (2026-08-19): user will source the 29 MANUALLY** (declined the web-sourcing round). Built a reusable bulk uploader → **`scripts/upload-photos.js`** (untracked; `node scripts/upload-photos.js <folder> [--dry-run] [--overwrite]`): scans a folder for images whose filename STARTS with the product id (`42.jpg`, `60-shito.png`), compresses to ≤900px JPEG (flattens transparency on white), uploads to `product-photos`, sets `products.img`; skips products that already have a photo unless `--overwrite`. Two manual routes given to user: (a) Admin → Inventory → per-product photo upload; (b) drop id-named images in a folder → I run the uploader. If web-sourcing is ever wanted after all, the round-2 plan stands (more Shopify/Woo GH feeds + manufacturer/Google → round-2 review page). (details of round 1 below)
- **(2026-08-19) PHOTO REVIEW ROUND 1 — build notes.** Re-confirmed via live Supabase query (`products.img` null): exactly **37 of 132 still blank** (id list in scratchpad `missing.json`; ids 41,42,43,60,64,67,70,71,75,77,81,83,90,95,101,102,103,105,108,109,111,112,114,121,124,129,131,135,136,137,138,151,152,154,155,156,160). Re-matched them against the two local feeds (`konzoom-all.json` 1500 + `bigsamps-all.json` 308) with a **brand-aware matcher** (manual brand map + word-boundary + bare-produce filter) → **18 have a same-brand candidate**, **19 only wrong-brand look-alikes**. Brands ABSENT from both feeds (→ will likely be "None fit" / manual): Abena, Cindy, Aunty Lulu's, Amoy, Diamir, Remie, Yanni, Kalyppo, Awake, HoH, Quaker, Bruggen, St Louis, Tetley, Ocean Spray, African Queen, Soda Crackers. Also a few "same-brand" are same-brand-WRONG-product (Heinz→corned beef, Welch's→juice, Golden Tree→choc bar not cocoa powder, One Yogo→strawberry not greek) — user will reject those. Downloaded+thumbnailed **119 candidate imgs** (sharp→280px JPEG, data-URI embedded) and built an interactive **Artifact review page** → https://claude.ai/code/artifact/394136d9-11d6-4fc1-a2b9-447ea58c571f — per-product photo tiles the user clicks to pick the correct match or "None fit"; exports an `id = cid` block (e.g. `90 = 90-0`) to paste back. **NEXT: user pastes picks → resolve cid→remote URL via `picks-resolve.json` → download each chosen img, compress, `db.uploadProductPhoto` + set `products.img`** (same path used for the original 95; see prev-session scratchpad `7dd7edab-…` upload scripts). For the ~19 "None" items, OFFER targeted web sourcing (manufacturer/retailer image) rather than leaving fully manual. **Scratchpad this session `3a63405b-…`:** `missing.json`, `candidates.json`, `imgmap.json`, `picks-resolve.json`, `match37b.js`, `fetch-images.js`, `build-page.js`, `review-template.html`, `review.html`. Feeds live in prev scratchpad `7dd7edab-…`.
- **(2026-07-21) CATALOG POPULATED — launch item #3 DONE.** Imported the triaged catalog: **132 products across 14 categories**, LIVE on sdg-mart.com (wiped the 36 placeholders via `scripts/import-catalog.js --wipe`). Used the sheet's **column-C rounded prices** (the user added a new rounded-price column C with a BLANK header, left of the original `Price (GHS)` col D — the CSV build maps C→Price) + 24 auto-selected bestsellers. `server.js`: `categories` → the 14 real ones, `essentials` re-pointed to `[46,45,37,132,79,140,141,78,99]`. `HomePage.jsx`: category tiles key images by category NAME (7 reuse `/icons/categories/*.jpg`, the other 7 render as dark tiles) + footer Shop links fixed. `sw.js` v70. Generated CSV: `Downloads/SDGMart-catalog-import.csv`. ⚠️ **Product photos: 95/132 done** (2026-07-21) — sourced from **konzoom.shop** (Shopify `/products.json`, 73) + **Big Samps** (`/wp-json/wc/store/v1/products`, 22), webp→jpeg via sharp, uploaded to Supabase + `products.img` set (matched by fuzzy name; wrong-brand matches excluded). **37 still blank** (mostly specialty sauces + some drinks/produce) — user is doing a manual review/fix pass. Match/upload scripts were run from `scratchpad` (konzoom-all.json / bigsamps-all.json / upload lists); Maxmart (maxmartonline.com) wasn't Shopify so unused. Also 7 category TILES still need photos. Re-import path if needed: rebuild the CSV from the xlsx (Python/openpyxl, Keep=Y rows, col C as price), `--dry-run`, then `--wipe`.
- **⭐ (updated 2026-07-21) START THE NEXT SESSION HERE →** Recently DONE: catalog populated (132 products / 14 categories, LIVE) · **product photos 95/132 LIVE** (37 blank — user's manual review pass; see the photo entry above) · both WhatsApp learning guides done + recolored to the dark theme (cookbook https://claude.ai/code/artifact/bf4d4f78-379e-4bd4-a625-2bbd797678b9 · applied build https://claude.ai/code/artifact/e12493da-2696-410e-b307-c43b41a588b4). **LIKELY NEXT — ask the user which:** (a) finish the **37 remaining product photos** (manufacturer/Google) + 7 category-tile photos; (b) **build the WhatsApp Claude agent** — Phase 0 is Meta-side (test number + keys), then Phase 1 the webhook (`GET`/`POST /api/whatsapp/webhook` + `sendWhatsApp` helper) with me in `server.js`; (c) rotating hero (6 images staged uncommitted in `icons/`); (d) launch-checklist bits (enable Birthday Gifts, iPhone smoke test, Render Starter + Supabase Pro upgrades, clean Supabase test data). Build the WhatsApp agent on **Claude** (`claude-opus-4-8` + Anthropic SDK **Tool Runner**; tools `search_products`/`add_to_cart`/`view_cart`/`place_order`(wraps `createOrderFromBody`)/`order_status`). Details continue below. Also note the old plan text that follows this line is superseded where it still says "cookbook guide is the next deliverable" — that's done. Reference: the **WhatsApp AI agent — now built on CLAUDE, not Gemini** (user has Anthropic credits + is learning via the cookbook; build with `claude-opus-4-8` + the Anthropic SDK **Tool Runner**). Plan + phased build path: memory `project_sdgmart_deferred.md` → WhatsApp entry (+ `project_gemini_agent_learning.md` pivot note). A cookbook-orientation guide with a 5-step learning path is done; the **"applied guide"** (mapping cookbook lessons → SDGMart WhatsApp phases + the exact tools `search_products`/`add_to_cart`/`view_cart`/`place_order`/`order_status`) is the next deliverable once the user finishes learning steps 1–3. WhatsApp launch decision: **option 1** (WhatsApp Business App → website link; greeting/away/Quick-Replies carrying the sdg-mart.com link) for now; **option 3** (custom Cloud API Claude agent on the Express server, writing to the same `orders` table) is the deferred flagship. ⚠️ Cloud API needs a dedicated number that then leaves the normal WhatsApp app.
- **(2026-07-21) Shipped this session — all LIVE on `sdg-mart.com` (auto-deploy now works):** (a) **persistent cross-device cart** — `carts` table (⚠️ `supabase-schema-cart.sql` RUN ✅) + GET/PUT `/api/me/cart`; `App.jsx` merges the server cart on sign-in, saves on change (debounced). (b) **more push-subscribe prompts** — reusable `NotifyOptIn` card (defined in `AccountPage.jsx`, attached to `window`) on the order-success screen + Account page. (c) **Sentry** — `@sentry/node` dep + init gated on `SENTRY_DSN` in `server.js` (DSN SET on Render ✅); forwards ONLY unhandled errors + process crashes (per-route caught 500s are NOT sent — offer to broaden to critical paths). (d) **admin push alerts** — `notifyAdmins()` (server.js, near `pushToUser`) pushes to every admin's devices on new order / order-issue / feedback / product-request; Dashboard Overview has an **"Enable admin alerts"** toggle. ⚠️ USER ACTION PENDING: tap it once on the phone to subscribe that device. (e) also: **self-hosted category images** (`/icons/categories/*.jpg` — dropped the Unsplash hotlink, fixes old browsers e.g. Win7) and Google description reworded to **"mobile money or cash on delivery"** (dropped "card"). Deferred, NOT shipped: **rotating homepage hero** (6 images staged UNCOMMITTED in `icons/` — see deferred memory).
- **(2026-07-21) ROOT CAUSE of the stalled deploys: Render Auto-Deploy was OFF.** Explains the whole mystery: the Jul-19 manual deploy (12:02 GMT) built v67 against a stale fetch, **v68 (OTP note + share tracking) never went live at all**, and the v69 push triggered nothing. Verified via live-bundle audit: prod was running exactly `dd310a8` (v67) — v67 markers present, v68/v69 markers absent; GitHub `main` confirmed at `6a8327e` (v69) the whole time. **Auto-Deploy toggled ON ("On commit") 2026-07-21**; this commit exists partly to trigger the first auto-deploy — it ships v68+v69 together (deploys are branch-tip snapshots; individual commits never need replaying).
- **(2026-07-19) iOS icon fix + squad tweaks** (`sw.js` v69): (1) iOS home-screen showed a stale old icon — added a dedicated `/icons/apple-touch-icon.png` (180×180, generated by `ensureIcons`) and pointed the apple-touch-icon `<link>` at the new versioned URL so iOS re-fetches instead of reusing its hard-cached copy (Android was already correct via the manifest). (2) SquadPage referral message now states the friend's first purchase must be **≥ GHS 50** for the GHS 5 credit — the logic already enforced this (referrer credited only inside the `firstOrderFree` gate, `FIRST_ORDER_FREE_MIN = 50`); this just makes it explicit. (3) **Removed the 5-member squad cap** (`database.js` `users.create`) — referred signups now always join the referrer's squad; SquadPage header shows `(n)` not `(n/5)`. ⚠️ Trade-off: the squad reward needs EVERY member to reach GHS 500, so larger squads make the goal harder. (4) **All icons rebranded to the real SDG wordmark** (`icons/SDG logo.png` → `icon-192/512.png`, `apple-touch-icon.png`, embedded `icon.svg`, each centered on black; icon URLs versioned `?v=69` in manifest/HTML/`sw.js` so devices refetch) — pre-generated via Windows System.Drawing and committed. NB: Render can't resize at boot (no `sharp`); `ensureIcons()` only fills MISSING files, so the committed logo icons win — regenerate with the scratch scripts if you change the logo. (5) **Homepage SEO + social**: added `<meta name="description">` (it was missing → Google was scraping the loading screen, hence the "unfinished" snippet) + Open Graph/Twitter tags + a 1200×630 `icons/og-image.png` share card, so `sdg-mart.com` links (including squad referral links) show a proper preview on WhatsApp/social. Snippet updates on Google's next crawl (expedite in Search Console → Request indexing).
- **(2026-07-19) Paystack WhatsApp-OTP note + shareable tracking for signed-in users** (commit 67748c8, `sw.js` v68): checkout shows a note under Pay Now that if the SMS OTP is slow, a "Get OTP via WhatsApp" backup appears after ~1 min. `/api/me/orders` now returns each order's `trackToken`; signed-in My Orders gained a "🔑 Share tracking" button (native-share/copy link) for active orders — previously the code was guest-only. Verified in preview.
- **(2026-07-19) Profile-nudge false-positive fix + squad rollover + 5-member cap** (commit dd310a8, `sw.js` v67): ProfileNudge was 401-ing on the address check (child effect ran before the session token was stored) and nagging COMPLETE profiles every sign-in → now uses the prop token, stays quiet if unverifiable, 7-day cooldown. Squad goal: spend OVER GHS 500 now rolls into the next round (member at 730 restarts at 230, not 0). Squads capped at 5 members (6th signup still records the referral, starts own squad). SquadPage shows capped progress + "banked" note + members (n/5). All verified.
- **(2026-07-19) Render build failure was a transient GitHub 403** (Render couldn't clone the repo — `git 403`, NOT Cloudflare, NOT code). Resolved itself on manual-deploy retry. First real live end-to-end **Paystack payment succeeded** (order #37, paid, ref recorded, pending_payment cleared) — the full live payment chain is proven.
- **Order-level reviews (2026-07-18):** the post-delivery prompt now asks ONE "Rate order SDG-xxxxx" per delivered order (items summarized underneath) instead of one prompt per product. `db.reviews.pendingForUser` returns unreviewed ORDERS (max 3); `createForOrder` stores product_id NULL; POST /api/me/reviews accepts orderId-only (ownership-checked; legacy productId path kept). ✅ `supabase-schema-order-reviews.sql` (§4.10) run + verified. Note: per-product star ratings were never displayed anywhere in the UI, so nothing visible was lost.
- **Legacy-domain redirect (2026-07-18):** GET page requests to `sdgmart.onrender.com` now 301 to `https://sdg-mart.com` (path+query preserved). Exempt: `/healthz` (UptimeRobot keep-awake must hit Render) and `/api/*` (Paystack webhook POSTs; onrender-origin PWA/service-worker API calls). Verified: pages redirect, deep links keep `?track=..&t=..`, exemptions hold, custom domain untouched.
> §1–§13 below are still accurate; this block is the current front line. `sw.js` CACHE_NAME = `sdgmart-v68-paystack-note-share-tracking`.
- **Route Batching bulk-assign SHIPPED (2026-07-18):** Admin → 🗺 Routes had a "Assign Rider" button per neighborhood group with **no onClick at all** — pure dead UI (the WORKING rider control is the per-order `<select>` dropdown in the Orders tab, `assignOrderToRider`/`POST /api/admin/orders/:id/assign` — that one was always fine). Wired the Routes button to a dropdown listing online riders (sorted by straight-line km to the group's first pinned order, reusing the same haversine formula the server uses for auto-assignment) — clicking a rider bulk-assigns EVERY order in that neighborhood group to them in one call (`Promise.all` of the existing per-order assign). **Verified live** end-to-end with a temp throwaway admin account (never touched the real admin password): 2 same-neighborhood orders + 1 online rider seeded → picker showed correct distance (0.5km) → click assigned both → group correctly vanished from the pending view; also verified the "No riders currently online" empty state. All test data deleted after.
- **Referrals investigated (2026-07-18) — not a bug, two things explain it:** (1) `referrals` table is 0 rows / no user has `referred_by` set — no referral signup→first-order chain has ever actually completed end-to-end in testing (signup + share-link code both verified wired correctly). (2) The leaderboard is genuinely month-scoped by design (`db.leaderboard.topReferrers` filters `.eq('month', currentMonth)`) — history isn't deleted, just not shown past the current month; last month's top referrer auto-awards GHS 15. **User decision confirmed: keep referral credit tied to the first order being ≥GHS 50** (same flag as §7's persistent free-delivery perk) — no code change made.
- **Recurring-orders cron SHIPPED (2026-07-18):** `runRecurringOrders()` in server.js, called from `runDailyJobs()` (still the same once/day-via-`/healthz` pattern, no real cron on this host). Finds `recurring_orders` where `active=true` and `next_run_at <= today`, places each via the SAME `createOrderFromBody` path as a normal checkout (so pricing/promos/loyalty/12pm-cutoff/first-order-free all apply identically) — this had been sitting as data+UI with no job actually running it. Details: drops items that no longer exist or are "Sold out" (stock 0 — the deliberate unavailable-flag in both stock-display modes) and pushes an "⏸ Auto-reorder skipped" notification if that empties the order; **always places Cash-on-Delivery** regardless of the customer's original payMethod (there's no saved card/MoMo to auto-charge — Paystack is popup-only); advances `next_run_at` by `cadence_days` **even on skip**, so a bad row can't retry forever/spam daily; per-row try/catch logs failures to `error_logs` (method `CRON`) instead of killing the whole batch; pushes "🔁 Auto-reorder placed" with a `/?track=<id>` link on success. **Verified live** with 3 seeded rows: happy path (order placed, cash forced even though the row's saved payMethod was 'paystack', next_run_at advanced by cadence), ghost-product row (skipped cleanly, next_run_at still advanced, zero CRON errors), not-yet-due row (left untouched). Bonus: this test user's recurring order was its first-ever completed checkout, and correctly triggered the first-order-free-delivery perk (§7) — confirms the two features compose correctly. All test rows/orders deleted after, `first_order_done` + the daily-job guard reset.
- **Portable tracking codes (2026-07-12), all verified:** success screen shows a save-me box with the full code `SDG-<id>-<20-hex-token>` + Copy button; the "Send Order to My WhatsApp" message now appends the code + live-track link; the guest Track Your Order page has a "Have a tracking code?" input that works on ANY device (validates against the server, remembers locally, opens live tracking; bad codes rejected); shared `/?track=<id>&t=<token>` links work on fresh devices and self-save; per-order "📋 Code" copy buttons. **Codes expire 7 days after delivery** (410 + friendly message) — exact via `orders.delivered_at` (new migration §4.9, stamped by `db.orders.setStatus`), falling back to delivery_date+1d. Guest "Track this order" button on the success screen now shows for guests too.
- **UX batch (2026-07-12), all verified in preview:** stock level hidden on ProductPage (low-stock shows "Almost sold out", no number); related products clickable (App passes `onView` to ProductPage); **Suggested for You is now real** (signed-in: frequency-ranked from own /api/me/orders; others see "Popular right now" from TOP_IDS_BY_ORDERS — was hardcoded ids [5,13,17,29]); footer: "Track Order & Delivery"→orders page, Returns→/terms#returns, About→/about (only WhatsApp/Contact still open WhatsApp); **guest order tracking** — HMAC track token (`orderTrackToken(id)`, derived from SUPABASE_SERVICE_KEY, no schema change) returned by createOrderFromBody, saved in localStorage `sdgmart_guest_orders` (max 10), tracking endpoint accepts `?t=` (401 without/with bad token), MyOrdersPage shows guests their device-local orders ("Track Your Order" view); **compact checkout step 1** — with a saved default/last-used address only name+phone show plus a "Delivering to <label>" card with "Change / add details" (expands full form; family mode always expands); cart: "Clear all" button in drawer + cart persists in localStorage `sdgmart_cart` across reloads AND sign-out; search: live (first keystroke jumps to filtered grid), ✕ clear button, auto-clears on opening a product.
- **Catalog caching (2026-07-12):** `/data/products.js` (loaded by every visitor, was ~700ms + 2 Supabase queries per request, server buckled at 20 concurrent) is now cached in memory for 60s (`_catalogCache` in server.js); admin product create/update/delete + settings save call `invalidateCatalog()` so edits appear immediately. Load-tested after: ~4,100 req/s at 50 connections, 10ms median, zero errors. ⚠️ Single-process cache — fine on one Render instance; revisit if ever scaled to multiple instances.

### SPA routing + Google Analytics (2026-07-12)
- **Google Analytics is live everywhere** — gtag (`G-D6LK26XSY0`) is in `SDGMart.html` (user added) AND `about.html`/`privacy.html`/`terms.html` (added + verified 2026-07-12: script loads, dataLayer populates).
- **The SPA now uses real URLs** (App.jsx `PAGE_PATHS`/`PATH_TO_PAGE`, `navigateTo` pushes real paths + fires `trackPageView` GA4 page_view): `/`, `/shop`, `/checkout`, `/squad`, `/my-orders`, `/account`, `/product`, `/cart`, `/track`, `/admin`. Order success → **`/order-confirmed`** (via `onOrderPlaced`/`markOrderConfirmed`) = the address to use as the **Google Ads conversion**.
- `server.js` has a catch-all (`app.get('*')`) serving the app shell for client routes (refresh/deep-link safe); API + real files fall through.
- **Fixed latent bug:** `SDGMart.html` loaded `data/products.js` + `responsive.css` via RELATIVE URLs → 404 + white-screen crash on deep paths → made absolute. (If a white screen with "Cannot read properties of undefined (reading 'map')" ever recurs, suspect a relative asset URL.)
- Verified end-to-end in preview: catch-all, cold-load deep paths load data, per-section page_views, `/order-confirmed` fires once on a real order, back button. NOT eyeballed: GA Realtime dashboard (needs live deploy).
- **GA4 `purchase` event DONE (2026-07-12):** `trackPurchase(snap, code)` in `CheckoutPage.jsx` fires on order success (both COD + Paystack, via `finishOrder`) → gtag `purchase` with `transaction_id`=SDG order code, `value`=order total (incl. delivery), `currency`=GHS, `shipping`, and `items[]` (item_id/name/category/price/quantity). Verified in preview: fires once on real order, correct fields. No double-count on refresh (/order-confirmed cold-loads to home). **User's GA setup TODO:** mark `purchase` as a Key event in GA4 + import to Google Ads (this is the accurate revenue conversion; the /order-confirmed page_view still works as a backup destination conversion).

### Shipped & live (previous session)
- **Feature 1 — Profile:** "👤 My Profile" added to the mobile menu (page existed but was unreachable); **birthday** (day+month) captured **once then locked** (server-enforced in `/api/me/profile`); a user's **first saved address auto-becomes default**; checkout **auto-fills** name/phone + the default address.
- **Feature 3 — Scheduled delivery:** checkout has a **"Deliver ASAP vs Schedule for later"** toggle → future date (≤7 days) + an **admin-editable time slot**; `createOrderFromBody` validates the 7-day window; admin + rider cards show the slot; slot editor in Admin → Settings; public `GET /api/delivery/slots`.
- **Feature 2 — Birthday gifts (⚠️ built + live but STILL OFF):** Admin **"🎂 Birthday Gifts"** tab = enable toggle + product multi-select (→ `app_config.birthday_gifts`). In a customer's **birth month** they add **one free gift** at checkout (server-validated, **once/year**). Daily happy-birthday **push** via `runDailyJobs()` fired from `/healthz`. **NEXT ACTION (user):** Admin → 🎂 Birthday Gifts → toggle ON + pick 2–3 products + Save → then run the final end-to-end gift check (test customer has a June birthday).
- **5 UX fixes:** add-address marks **Label*/Neighborhood*** required; **delivered orders** show a thank-you + "Order again" (no stale map/notify) on `OrderTrackingPage`; **track page** shows the chosen slot + delivery date; report success says **"We'll reach out soon"**; **admin overview crash FIXED** (it rendered the raw `items` array → React crash; now a summary string, 10 recent rows) + **"Sign out & restart"** on the error screen.
- **Security + perf code review — all 10 findings + 2 follow-ups fixed & deployed:**
  1. **CRITICAL — server-authoritative pricing.** `computeOrderPricing(reqUser, body)` recomputes item prices (DB) + promos + squad discount + loyalty(capped) + delivery + total. Both `/api/paystack/init` (charge amount) and `createOrderFromBody` (stored order) use it — **client `price`/`total`/`amount` no longer trusted** (was: pay GHS 0.01 for any cart).
  2. `express.static(__dirname)` no longer serves source/docs (deny-middleware 404s `/server.js`, `/database.js`, `*.md`, `*.sql`, `package.json`, `/components/*`). 3. Riders blocked from `/api/me/*` (`customerOnly`) — was overwriting the customer with the same id. 4. 5-min cache (`getOrderItemCounts`) for the per-pageload top-seller scan. 6. gift claim set **after** create. 7. exported `db.rowOut/rowsOut`. 10. `ensureIcons` writes only missing files.
  - **Stock-decrement admin toggle** (`app_config.deduct_stock`, Admin → Settings, **OFF by default** — turn ON only when SDGMart holds its own stock; partners supply now).
  - Follow-ups: `/api/orders` bounded to recent **500** (`?limit`); **CORS locked** to `sdg-mart.com`/`www`/`sdgmart.onrender.com`; removed duplicate **"Kalpohini"** (kept "Kalpohin").
- **New migration run:** `supabase-schema-tweaks.sql` (birthday cols, `orders.delivery_slot`, `app_config` seeds) — applied in Supabase.

### 🟡 IN PROGRESS — catalog population (the active task)
User chose a **full wipe** of placeholder products → load the real catalog → then attach images.
- **Research delivered:** `C:\Users\Solo\Downloads\SDGMart-catalog-research.csv` — **254 Ghanaian grocery products, 16 categories**, from konzoom.shop et al.
- **✅ Triage workbook built (2026-07-12):** `C:\Users\Solo\Downloads\SDGMart-catalog-triage.xlsx` — Catalog sheet (Y/N dropdowns for Keep?/Bestseller?, pre-guessed Unit column, optional Image URL column, autofilter) + READ ME sheet (instructions, example row, live kept-count counters). **NEXT ACTION (user): fill the Keep? column** (adjust prices/units, add missing rows), hand it back.
- **✅ Import script built + dry-run tested (2026-07-12):** `scripts/import-catalog.js`. Usage: `node scripts/import-catalog.js <catalog.csv> [--wipe] [--dry-run] [--stock 100] [--images-dir <dir>]`. Reads a CSV export of the triage sheet (Claude converts the xlsx → CSV); imports only Keep=Y rows; `--wipe` required for full replace (refuses if products exist without it); default stock 100 (stock 0 shows "Sold out"); images from Image URL / local `--images-dir` (matched by row# or name slug), compressed to 900px JPEG via optional `sharp` (`npm i --no-save sharp`) using `db.uploadProductPhoto` + `db.products.create`. Aborts on invalid rows; prints the final category list at the end.
- **Images:** source **manufacturer-first** photos for the kept set (copyright caveat given; user does final review — inherently imperfect, candidates + cleanup).
- **Hardening:** the source-file deny middleware in `server.js` now also 404s `/scripts/*` (CACHE_NAME → `sdgmart-v50-scripts-deny`).
- ⚠️ **After a full replace:** update the app's **category list** (currently 9, hardcoded in `server.js` `categories` array) to the final set, and **re-point `ESSENTIALS`** (hardcoded product ids in `server.js` ~line 349) since ids change on reinsert. Then bump CACHE_NAME + deploy.

### Shipped 2026-07-12 (this session; CACHE_NAME `sdgmart-v52-feedback-retention`)
- **First-order free delivery ≥ GHS 50 + persistent perk** (see §7) — small first orders no longer consume it; referral credit now fires on the first *qualifying* order.
- **FeedbackBox** (`components/FeedbackBox.jsx`): "Spotted a problem? Tell us." card in the HomePage footer + Account page — in-app send (`POST /api/feedback`, rate-limited 5/10min) lands in Admin → Issues as "💬 General feedback (from <name>)"; WhatsApp button always available. Migration run + **verified end-to-end 2026-07-12** (insert → admin join with sender name → test row cleaned up).
- **Admin → 🔁 Retention tab**: `GET /api/admin/retention` (last-6-months active/returning/new + rate; lapsed = no order in 30+ days, capped 500) + `POST /api/admin/retention/notify` (win-back push, editable message, only reaches 🔔 push subscribers). Logic verified read-only against prod data; **tab UI not yet eyeballed (needs admin login)**.

### Still pending at launch (updated 2026-07-19)
Done: ✅ Paystack LIVE + **first real payment succeeded end-to-end** (order #37) · ✅ referrals SQL · ✅ recurring-orders cron · ✅ **Cloudflare orange-cloud flip LIVE** · ✅ **Cloudflare cache set to "Respect Existing Headers"** (verified on prod: bundle `max-age=300`, sw.js `no-cache`) — no more per-deploy purge needed · ✅ Retention tab eyeballed · ✅ GA4 key-event setup done · ✅ all migrations through §4.10.
**Remaining, in order:**
1. **Enable Birthday Gifts** (Admin → 🎂 → ON + pick products; test customer's June birthday passed — set a July birthday on a test account to e2e-test).
2. **Real iPhone smoke test** (Paystack popup, Google sign-in, home-screen install, tracking) — the last untested surface.
3. **Catalog population** — the big one: user fills Keep? column in `SDGMart-catalog-triage.xlsx` → then import (script ready), source images, update `categories` + `ESSENTIALS` in server.js.
4. **Render Starter + Supabase Pro upgrades** right before launch (no code changes needed; set Supabase spend cap to allow overages).
5. **Clean test data** in Supabase: users `sdgtest-…@example.com` (id 6) + `sdgtest-firstorder@example.com` (id 9, incl. its saved address + any leftover reviews), test orders ~ids 20–23.

---

## 1. What & where

- **Local project dir:** `C:\Users\Solo\Downloads\SDGMart`
- **Repo:** https://github.com/Solomonowusuoa/SDGMart (branch `main`)
- **Custom domain:** `https://sdg-mart.com` (bought on Cloudflare; note the hyphen). `https://sdgmart.onrender.com` still works and stays active.
- **Hosting:** Render, auto-deploys on every `git push` to `main`.
- **The codebase is domain-agnostic** — all API calls are relative, referral links use `window.location.origin`, push uses `self.location.origin`. Switching domains needs **no code change**, only external config (see §13).
- **Admin login:** `solomonowusuoa@gmail.com` (default pw was `sdgadmin2026`, changed on first login; use “Forgot password” if lost)

## 2. Tech stack (and the non-obvious bits)

- **Frontend:** React 18 (UMD via CDN) written as `.jsx` files using **global-window components** (no imports/modules). Each file defines components and does `Object.assign(window, { Foo })`. They reference each other as bare globals.
- **No Babel in the browser.** The server bundles all source files with **esbuild** (`/app.bundle.js`), built once at startup and rebuilt only when a source file's mtime changes (see `server.js` → `BUNDLE_FILES`, `buildAppBundle`). `SDGMart.html` loads just React + `/app.bundle.js` + `/data/products.js`.
- **Backend:** Node/Express (`server.js`), single process.
- **DB:** Supabase (Postgres) via `@supabase/supabase-js` using the **service_role key** (bypasses RLS). All DB access is in `database.js` (async methods, snake_case↔camelCase mapping via `rowIn`/`rowOut`).
- **File storage:** Supabase Storage bucket `product-photos` (product images, compressed client-side to ~900px JPEG before upload).
- **Maps:** Leaflet + **OpenStreetMap tiles** for display; **LocationIQ** for search + reverse-geocoding (key in `window.LOCATIONIQ_KEY`, falls back to Nominatim if unset). See `components/MapPicker.jsx` (`sdgMapTileLayer`, `sdgGeocoder`).
- **Payments:** **Paystack** (card + mobile money). Inline v2 popup → server `init`/`verify`/`webhook`. See §6.
- **Email:** Resend (optional; only used for password reset now — signup verification was removed).
- **Push:** Web Push (VAPID). `sw.js` handles `push`/`notificationclick`.
- **PWA:** `manifest.json`, `sw.js`. `CACHE_NAME` is **stamped automatically at serve time** from the git commit (audit G-05) — do not bump it by hand; the literal in the file is a placeholder.

### Client globals injected by `/data/products.js`
`window.PRODUCTS`, `CATEGORIES`, `ESSENTIALS`, `NEIGHBORHOODS`, `TOP_IDS_BY_ORDERS`, `SHOW_FRESHNESS`, `LOCATIONIQ_KEY`, `PAYSTACK_PUBLIC_KEY`, `PROMO_MAP` (set by App.jsx), and helper `window.orderCode(id)` → `SDG-00017` (from `hooks.js`).

## 3. Environment variables (Render → Environment, and local `.env`)

`.env` is gitignored; `.env.example` documents them. Required/used:
- `SUPABASE_URL`, `SUPABASE_SERVICE_KEY` — **required**
- `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT` — web push
- `LOCATIONIQ_KEY` — maps/geocoding (publishable; domain-restricted in LocationIQ dashboard)
- `PAYSTACK_SECRET_KEY`, `PAYSTACK_PUBLIC_KEY` — payments (online pay option only shows when BOTH are set; `/api/paystack/config` reports `enabled`)
- `RESEND_API_KEY`, `RESEND_FROM_EMAIL` — optional email
- `GOOGLE_CLIENT_ID` — optional Google sign-in (set OAuth consent screen to "In production" to avoid the 100-user cap)
- `NODE_ENV` — not required (bundle caching is mtime-based)

**Server reads `.env` from its own dir** (`require('dotenv').config({ path: __dirname + '/.env' })`) so it works regardless of cwd.

## 4. Supabase SQL files — run in this order (idempotent, safe to re-run)
> ⚠️ **Since 2026-08-29 use `node scripts/migrate.js status` instead of this list.** It records what
> has actually been applied in a `schema_migrations` table rather than tick marks in prose — which
> is what let `supabase-schema-cart.sql` be marked run below when it never was, leaving the
> cross-device cart silently broken. Three files were added after this list: `-order-idempotency`,
> `-indexes-and-email`, `-constraints`. Run migrations on STAGING first.
1. `supabase-schema.sql` (base tables; RLS enabled)
2. `supabase-schema-additions.sql` (addresses, reviews, issue_reports, promotions, user/product columns)
3. `supabase-schema-requests.sql` (product_requests)
4. `supabase-schema-ops.sql` (users.referral_count, error_logs)
5. `supabase-rls-fix.sql` (enable RLS everywhere)
6. `supabase-schema-paystack.sql` (orders.paid, orders.paystack_ref, pending_payments)
7. **`supabase-schema-referrals.sql`** ✅ confirmed run (verified 2026-07-12: referrals table + users.referred_by exist)
8. **`supabase-schema-feedback.sql`** ✅ run 2026-07-12 (issue_reports.order_id nullable; general FeedbackBox verified end-to-end)
9. **`supabase-schema-delivered-at.sql`** ✅ run 2026-07-12 (orders.delivered_at — exact tracking-code expiry + fixes "Completed at" on the tracking page)
10. **`supabase-schema-order-reviews.sql`** ✅ run 2026-07-18 (verified by probe insert — order-level reviews fully live)

## 5. Day-to-day workflow
1. Edit files locally.
2. Validate the bundle compiles:
   `node -c server.js && node -c database.js` and an esbuild transform of all `BUNDLE_FILES` (see git history for the one-liner).
3. Verify in the Claude **preview tool** (`preview_start` name `sdgmart`, runs `node server.js` on the local `.env`). Note: locally Paystack shows `enabled:false` (no keys) and LocationIQ falls back to OSM — that's expected.
4. ~~Bump `CACHE_NAME` in `sw.js`.~~ **No longer needed (2026-08-29, audit G-05).** The server stamps the deployed build id into `sw.js` as it serves it — from `RENDER_GIT_COMMIT`, else `.git/HEAD`, else the newest file mtime. The literal in the file is a dev placeholder. This step is gone because a release step that depends on remembering eventually runs without it, and this one failed silently.
5. Commit (author: `solomonowusuoa@gmail.com`, co-author line per house style) and `git push`.
6. Render auto-deploys (~2 min). Hard-refresh (Ctrl+Shift+R) to clear the service worker.

**GitHub auth:** the stored token has expired before. If push fails with "Invalid username or token", create a classic PAT with `repo` scope and `git remote set-url origin https://USER:TOKEN@github.com/Solomonowusuoa/SDGMart.git`.

## 6. Paystack flow (how it works)
- Checkout shows **"Pay Now (Card/MoMo)"** (if `enabled`) and **Cash on Delivery**.
- `POST /api/paystack/init` → server calls Paystack initialize (amount locked, pesewas, GHS), stashes the order draft in `pending_payments`, returns `access_code`.
- Client opens `js.paystack.co/v2/inline.js` popup → `resumeTransaction(access_code)`.
- `onSuccess` → `POST /api/paystack/verify` → server verifies, then `createOrderFromBody(... paid:true, paystackRef)` (idempotent). 
- `POST /api/paystack/webhook` (HMAC-SHA512 verified using `req.rawBody`) is the safety net if the customer's tab closes.
- **Going live:** REPLACE test keys with `sk_live_`/`pk_live_` (same var names), set the webhook URL to `https://sdgmart.onrender.com/api/paystack/webhook`, ensure the Paystack account is **activated** and a settlement account is set.

## 7. Order model notes
- Real order id is a Postgres bigserial. Displayed everywhere as **`SDG-<id>`** via `window.orderCode(id)`.
- Tracking uses the numeric id (`/api/orders/:id/tracking`). `createOrderFromBody` ignores any client-sent `id`.
- New orders are `status: 'queued'`, unassigned. **Riders only see orders the admin assigns** (`/api/admin/orders/:id/assign`). Riders see `forRider` (assigned/in_transit only).
- First order (signed-in) → free delivery **only when the order is ≥ GHS 50** (`FIRST_ORDER_FREE_MIN`, after squad discount + loyalty; mirrored in `server.js` `computeOrderPricing` and `CheckoutPage.jsx`). **The perk persists**: `first_order_done` is set only when a qualifying (≥50) order is placed, so small first orders don't burn it. Referral credit rides the same flag → the referrer is credited on the referee's first **qualifying** order (also blocks GHS-1 referral farming). Guests can never get it (no session → `reqUser` null on every path).

## 8. Referrals & leaderboard (current behaviour)
- Signup with a code stores `referred_by` (NO immediate credit).
- On the referee's **first order**, `db.referrals.creditFirstPurchase` gives the referrer **GHS 5** loyalty + logs a row in `referrals` tagged with `YYYY-MM`.
- Leaderboard is **monthly** (`db.leaderboard.topReferrers` counts current-month `referrals`). `db.leaderboard.awardLastMonthWinner` (called opportunistically from `/api/leaderboard`) gives last month's top referrer **GHS 15**, idempotent via `app_config` key `leaderboard_awarded_month`.
- All credit lands in one `loyalty_balance` (shown as the ⭐ pill + checkout toggle).

## 9. Admin panel (`components/AdminPage.jsx`) tabs
Overview, **Dashboard** (revenue/orders charts — inline SVG, no chart lib), Orders (filter/search/assign-rider/delete, PAID vs COLLECT badge), Inventory (photo upload + low-stock threshold), Expiry, Routes, Riders (create rider accounts), Promotions (create/publish flash sales → push), Requests (product requests), Issues (problem reports), Analytics (search queries), Leaderboard, Comms, Errors (server + client crash logs), Settings (freshness toggle), Security. *(Payments tab was removed.)*

## 10. Recently fixed (context for "why")
- **Orders silently failing yesterday:** Paystack code wrote `paid`/`paystack_ref` before `supabase-schema-paystack.sql` was run → every insert failed (no orders saved, counter frozen, first-order-free never cleared). Fixed once SQL ran. An **error boundary** (App.jsx `AppErrorBoundary`) + `/api/client-error` logging now surface such issues in the admin Errors tab instead of blanking the page.
- Tracking dead-end (random code mismatch), back-button-exits (SPA history), blank LocationIQ map (now OSM tiles), email-verification removed, order codes unified.

## 11. OPEN / PENDING — start here next session
0. ✅ **DONE (2026-06-18) — Custom domain migration (`sdg-mart.com`).** All 7 external configs done per §13: Render custom domains; Cloudflare grey-cloud CNAMEs (`@` + `www` → `sdgmart.onrender.com`); LocationIQ origins; Paystack webhook → `https://sdg-mart.com/api/paystack/webhook`; UptimeRobot kept on onrender `/healthz`; Resend domain verified (`RESEND_API_KEY` + `RESEND_FROM_EMAIL=SDGMart <noreply@sdg-mart.com>` set on Render); Google OAuth JS origins. Live at `https://sdg-mart.com` (cert issued; www→apex 301). `sdgmart.onrender.com` still active. Code unchanged (domain-agnostic).
1. ✅ **DONE (2026-06-18) — Google OAuth brand verification APPROVED.** Unverified-app warning gone, 100-user cap lifted. Search Console domain verified via the Google↔Cloudflare auto-integration (root TXT `google-site-verification=yOXxtnZiB8kVlHrueoBG-JuYPMUMtM8vdkFxadW9xFE` — **do NOT delete**). Consent screen: name/logo (`icons/icon-512.png`)/support+dev email; homepage = `https://sdg-mart.com/about`, privacy = `/privacy`, terms = `/terms`. First submission rejected (homepage was behind the login wall + didn't explain purpose/Google data) → fixed by adding the public **/about** landing page → resubmitted → approved.
   - **New static pages added this session:** `about.html` (`/about`, public homepage that explains purpose + Google Sign-In data usage), `privacy.html` (`/privacy`), `terms.html` (`/terms`); routes in `server.js`; footer "Privacy"/"Terms" links in `components/HomePage.jsx`. `sw.js` CACHE_NAME now `sdgmart-v44-about-tweak`. These are server-rendered standalone pages (NOT in the React bundle) so Google's reviewers fetch real HTML.
2. **Run `supabase-schema-referrals.sql`** in Supabase (required for §8 to work) — if not already done.
3. **Swap Paystack to live keys** + set webhook to `https://sdg-mart.com/api/paystack/webhook` + confirm account activated.
4. **Verify post-deploy:** place a test order → appears in admin Orders with `SDG-` code + PAID/COLLECT badge; "Track" works; back button doesn't exit; admin refresh doesn't blank.
5. **Clarify & implement "admin dashboard items clickable to their pages"** — target was ambiguous (Dashboard top-products list? KPI cards? inventory rows?). Ask which items and where they should link.
6. **Deferred backlog** (see memory `project_sdgmart_deferred.md`): WhatsApp Cloud API store-bot; dedicated `TRACK_TOKEN_SECRET` env var (decouples guest tracking codes from SUPABASE_SERVICE_KEY); "find my order by code + phone" recovery; more push-subscribe prompts; saved cart/wishlist; phone/SMS login; neighborhood social proof; 2FA admin; audit log; Sentry (SENTRY_DSN hook ready); Dagbani/Twi i18n.

## 12. Free-tier ceilings (when to pay)
- Resend (100/day) — first to hit if order-confirmation emails are added.
- Supabase egress 5GB/mo — product images (already compressed); DB 500MB.
- LocationIQ 5,000 geocodes/day (generous).
- Render free: 512MB/slow CPU, kept awake by **UptimeRobot** pinging `/healthz`.
- Roughly ~$50/mo total once busy (Render $7 + Supabase $25 + Resend $20).

## 13. Custom domain migration — `sdg-mart.com` (Cloudflare)
No code changes needed; all external config. Do in this order:
1. **Render** → Settings → Custom Domains → add `sdg-mart.com` + `www.sdg-mart.com`. Render shows the DNS record(s) to create.
2. **Cloudflare** → DNS → add the records Render gave (root usually CNAME→`sdgmart.onrender.com` with flattening, plus `www`). Set proxy to **DNS only (grey cloud)** to avoid the Cloudflare↔Render SSL loop (or use SSL mode Full(strict) if proxied). Render then issues SSL → `https://sdg-mart.com` live.
3. **LocationIQ** → token → Allowed Origins → add `https://sdg-mart.com` + `https://www.sdg-mart.com` (keep onrender). *Maps/search break on the new domain without this — the key is domain-restricted.*
4. **Paystack** → webhook URL → `https://sdg-mart.com/api/paystack/webhook`.
5. **UptimeRobot** → keep the keep-awake monitor on `https://sdgmart.onrender.com/healthz` (hits Render directly — immune to the future orange-cloud flip, where a cached `/healthz` on `sdg-mart.com` could be answered by Cloudflare's edge and never wake Render). Optionally add a *second* monitor on `https://sdg-mart.com/healthz` to alert on public-domain (DNS/cert/Cloudflare) failures.
6. **Resend** → add domain `sdg-mart.com` → add its SPF/DKIM/DMARC records in Cloudflare → verify → set Render env `RESEND_FROM_EMAIL=SDGMart <noreply@sdg-mart.com>`.
7. **Google OAuth** → Credentials → Authorized JavaScript origins: add `https://sdg-mart.com` + `https://www.sdg-mart.com`; consent screen → Authorized domains: `sdg-mart.com`. (Then proceed to §11.1 verification.)
- Keep `sdgmart.onrender.com` active throughout; both URLs serve the same app. Existing web-push subscriptions are tied to whichever origin the user subscribed on.
- **LAUNCH-TIME FOLLOW-UP — switch Cloudflare DNS records to proxied (orange cloud).** Currently DNS-only (grey) for a clean setup + testing. At launch, to gain Cloudflare CDN caching + DDoS protection + origin-IP hiding: **(1) FIRST** set Cloudflare → SSL/TLS → Overview → **Full (strict)** (Render serves a valid cert, so this is safe); **(2) THEN** flip both records (`@` and `www`) to orange. ⚠️ Flipping to orange while SSL mode is "Flexible" = infinite redirect loop (site down). ⚠️ After going orange, **purge Cloudflare cache after every deploy** (or add a cache rule that bypasses the HTML + `/app.bundle.js` + `/data/products.js`), otherwise users get the stale bundle even after bumping `sw.js` CACHE_NAME.

---
*Last updated 2026-06-18 (commit `e70bf4d`): domain migration COMPLETE + Google OAuth verification APPROVED; added public /about homepage + /privacy + /terms pages. Remaining open: run `supabase-schema-referrals.sql` (§11.2); swap Paystack to live keys + activate account (§11.3); post-deploy test order (§11.4); launch-time Cloudflare orange-cloud flip (§13). Bump this doc as things change.*
